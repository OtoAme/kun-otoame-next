# 模块 01：后台地基与待审事项（实施计划）

状态：设计方向已于 2026-09-10 定稿（第 12 节），用户管理确定早迁；核心功能、统计首页与用户管理早迁已实现，2026-09-11 站长确认当前体验验收通过。当前已达到上线候选水平，最终技术检查见第 10 节；尚未生产部署，上线后仍需完成一轮真实审核。配对文档：[PM 版](./01-admin-foundation-pm.md)。上位文档：[总计划](./00-master-plan.md)第 4.2 节（M01-1 至 M01-5）、[运营层设计基线](../7.operations-layer-redesign.md)第 2.2、7.1 至 7.3 节。实施前核查与当前实现分别在对应段落标明；待审事项已完成模拟单元测试、真实 PostgreSQL / Redis 场景与实际界面联调；各实验的证据和剩余上线条件见第 9、10 节。

2026-09-10 已确认稳定管理页面可在核心开发基本完成后提前迁移，执行边界见第 6.9 节。本文其余数据、接口、文件与任务清单描述 01 核心交付；早迁从既有管理页任务中逐项承接，不增加首批必交范围。D1、D2 的决定已写入第 12 节，D3 的实际锁定依赖已记于第 6.3 节，已完成两套 CSS 产物及实际加载边界核对，见 E01-01。

## 1 目标与基线

| 目标                                                                                       | 依据              |
| ------------------------------------------------------------------------------------------ | ----------------- |
| 后台在独立根布局与独立样式入口下只用 shadcn，前台保持 HeroUI v2                            | 基线 7.2          |
| 待审条目、待审资源、反馈、举报进同一队列；左列表右详情；确认后处理；键盘导航；今日已处理计数 | 基线 7.1          |
| 审核投稿与资源申请时能看到发布后的前台真实样式（投稿页面与资源卡片），可内嵌可独立打开     | 基线 7.3          |
| 后台队列门槛统一到管理员级；封禁、删条目、站点设置、群发邮件保留超级管理员                 | 基线 2.2          |
| 旧反馈与旧举报在待审事项只读，处理仍在旧后台；旧后台过渡期完整可用                           | 总计划 3.3、4.2   |
| 每来源按等待时长取候选，最老事项不被截断掩盖                                               | 总计划 4.2 缺口一 |
| 今日已处理按成功的待审事项动作与业务对象统计，排除无关日志                                   | 总计划 4.2 缺口二 |

当前前台根布局 [app/(site)/layout.tsx](../../app/(site)/layout.tsx) 导入 [styles/index.css](../../styles/index.css)，后者导入 [styles/tailwind.css](../../styles/tailwind.css)（`@plugin './hero.ts'` 加载 HeroUI、`@custom-variant dark`）与 blog、prose、editor、themes，并含全局 `* { font-family … !important }`；`app/(site)/admin/layout.tsx` 嵌套其下；新控制台根布局 `app/(dashboard)/layout.tsx` 独立导入 `styles/dashboard.css`。本模块不新增表、字段、索引、定时任务与基础设施。

## 2 偏离与理由

| 对象                                 | 现状或原文                                                                  | 本文做法                                                             | 理由                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------ | --------------------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 审核确认 | 后台审核入口 | 所有审核动作，包括鼠标与快捷键入口，均先确认再提交 | 统一防止误操作；确认展示对象、结论与理由，资源拒绝与投稿判违规另明确不可逆后果。沿用原处理语义和角色门槛，详见 D2-3。 |
| 首批待审事项范围                       | 总计划四来源、旧项只读                                                      | 四来源；旧反馈、旧举报只读，处理入口链接旧后台；创作者申请不入队     | D1 已确定四来源与旧项只读；旧后台在过渡期完整可用，这两类在那里处理                                                                                                                                                                                                                                                                        |
| 预览承载                             | 基线 7.3：复用前台根布局                                                    | 本文按「预览放在前台根布局下」编写                                   | 基线 7.3 原文即「在前台根布局下新增仅管理员可访问的预览路由」，复用既有客户端外壳条件分支代价更小。D2-5 已确定复用前台根布局，两个预览共用相同承载方式                                                                                                                                                                                     |
| 基线未规定                           | 后台每页服务端动作加客户端接口双通道                                        | 控制台统一走 HTTP API，管理业务沿用 `/api/admin/*`                   | 现状双通道鉴权与校验各写一遍且门槛已漂移。旧后台不改。列 D2-2                                                                                                                                                                                                                                                                              |
| 资源申请列表按浏览者 NSFW 偏好过滤   | [get.ts](../../app/api/admin/resource-apply/get.ts) 把 NSFW 头当 where 条件 | 待审事项候选查询不套该过滤                                             | 站长的浏览偏好会把待审资源挡在队列外，最老项永久缺席。旧页面行为不变                                                                                                                                                                                                                                                                       |
| 资源申请处理先读后写                 | 事务外读 status，事务内无条件 update 或 delete                              | 事务内改条件更新与条件删除                                           | 满足 M01-4「重复动作不重复处理」的必要正确性修复，对外契约不变                                                                                                                                                                                                                                                                             |
| 投稿结算先于状态抢占 | 通过、驳回、违规路径在抢占 pending 前调用押金结算 | 将 `claimPending` 移至各自结算之前，通过仍保留 `publishCore → claimPending` 顺序 | 带押金的真实并发会让落败方先碰到已结算 reservation，偏离既有 409 冲突契约。先抢占再结算，使落败方在账务调用前返回状态冲突；不改账务原语、奖励规则、schema 或外部契约。 |

## 3 术语

| 术语       | 定义                                                                                               |
| ---------- | -------------------------------------------------------------------------------------------------- |
| 路由组     | Next App Router 中括号命名的目录，不进 URL，只组织布局                                             |
| 根布局     | 直接渲染 `<html>`、`<body>` 的布局；删掉顶层布局后每个路由组各有一个，跨根导航是整页加载           |
| 控制台     | 新后台，前缀 `/dashboard`（D2-1），路由组 `app/(dashboard)`，组件目录 `components/dashboard`       |
| 旧后台     | 现有 `app/(site)/admin/*` 与 `components/admin/*`，HeroUI 实现，本模块只减不加                            |
| 来源       | 待审事项项出处：`submission`、`resource-apply`、`feedback`、`report`                                 |
| 项目标识   | `来源:主键`，用于选中、深链接与动作路由                                                            |
| 等待起点   | 计算等待时长的时间字段，按来源定义，见 4.2                                                         |
| 候选上限   | 每来源一次取回的最多条数，超出即标记截断                                                           |
| 只读来源   | 在待审事项可见可查，但不提供处理动作，动作入口链接旧后台                                             |
| 今日已处理 | 当前管理员在上海时区自然日内成功完成的待审事项动作次数，按动作逐次计数，数据源是不可变的专用处理日志 |

## 4 数据模型

### 4.1 变更范围

无新表、无新字段、无新索引、无迁移脚本、无 Prisma schema 改动。待审事项是只读聚合视图；写入面只有投稿与资源申请两类既有服务。唯一的数据层面新增是 `admin_log.type` 的三个新取值（该列是自由字符串，不需要迁移）。

### 4.2 来源定义（现状核查加拟定谓词）

| 来源         | 待处理谓词                                                                                      | 等待起点                                         | 标识                  | 现状排序                                                                                                                 | 待审事项所需                                |
| ------------ | ----------------------------------------------------------------------------------------------- | ------------------------------------------------ | --------------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------- |
| 投稿         | `patch_submission.status = 'pending'`                                                           | `COALESCE(submitted_at, created)`，见下方第 4 条 | `submission:{id}`     | pending 已是 `submitted_at asc, id asc`（[service.ts](../../app/api/admin/patch-submission/service.ts) 第 99 至 108 行） | 谓词沿用，排序与等待起点统一按 `COALESCE` |
| 资源申请     | `patch_resource.status = 2`                                                                     | `created`                                        | `resource-apply:{id}` | `created desc`，且带 NSFW 过滤                                                                                           | `created asc, id asc`，不带 NSFW 过滤     |
| 反馈（只读） | `user_message.type = 'feedback'`、`recipient_id IS NULL`、`sender_id IS NOT NULL`、`status = 0` | `created`                                        | `feedback:{id}`       | `created desc`，且不过滤 `status`                                                                                        | 补 `status = 0`，`created asc, id asc`    |
| 举报（只读） | `patch_report.status = 0`，两种 `target_type` 一次取全                                          | `created`                                        | `report:{id}`         | `created desc`，`targetType` 必须逐个指定                                                                                | `created asc, id asc`                     |

四条必须写进设计的现状事实：

1. [getFeedback](../../app/api/admin/feedback/service.ts) 不过滤 `status`，其 `total` 含已处理行，待审事项不能拿它当待办来源或计数口径。
2. `getReport` 的 where 固定 `target_type: targetType`，默认 `comment`，一次只能取一种目标类型。
3. `getPatchResourceApply` 的 limit 上限来自 `adminPaginationSchema`（最大 100），且把 NSFW 头作为 `patch` 的 where 条件。
4. 现有 pending 排序是 `submitted_at asc`，而 Postgres 的 ASC 默认 NULLS LAST，所以「先按 `submitted_at asc` 取前 50、再在应用层回落 `created`」会把 `submitted_at` 为空的最老行留在候选窗口之外。提交动作在 draft 或 changes_requested 转 pending 时会写入 `submitted_at`（`app/api/patch-submission/submit.ts` 第 260 至 271 行），但该列在 schema 上可为空，历史行无法保证。待审事项候选因此统一按 `COALESCE(submitted_at, created), id` 在同一条查询里排序，等待起点用同一表达式：正常数据下与现排序结果一致，空值行也不会被挤出窗口。这不构成新的二选一决策，也不需要数据清理。

因此：四类候选都由待审事项服务按上表谓词与第 4 条的 `COALESCE` 排序自行查询与计数（投稿的审核角色门槛在 route 层显式校验，与 `listAdminPatchSubmissions` 的门槛一致），不修改既有列表服务，旧页面行为不变。

行标识为「来源加主键」，举报一行一条记录；被举报目标下的其他待处理举报作为详情上下文按需读取，队列层不做分组统计。现状 `handleReport` 是按目标批量处理（同目标 `status = 0` 的举报一起置位并逐个发通知，见 [service.ts](../../app/api/admin/report/service.ts) 第 207 至 264 行），本批举报只读故不受影响。

### 4.3 今日已处理

口径：当前管理员在上海时区自然日内成功完成的待审事项动作次数；数据源只有 `admin_log` 的专用 type 行，每次成功动作恰好一条。不读 `patch_submission` 的 `reviewed_at` 与 `reviewed_by_id`：这两个字段可变，「要求修改 → 作者重提 → 由另一位管理员或次日由本人再审」会把它们覆盖，前一位管理员当日的处理记录随之消失；也不用「当天 admin_log 条数」，因为会社诊断、发放萌萌点、改设置都写同一张表。

| 动作                             | 实施前日志                                                                                                                                                                                                        | 本模块                                                                                                                                            |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 投稿通过、要求修改、驳回、判违规 | [review.ts](../../app/api/patch-submission/review.ts) 的 `writeAdminLog`（第 59 至 66 行）固定写 `type: 'update'`，四个动作各写一条；`writeCompanyResolutionDiagnosticLogs` 复用同一个 helper，每条诊断再写一行 | 给 `writeAdminLog` 加可选 type 参数（默认 `'update'`），只在四个动作的那一条日志上传 `submission_review`；诊断日志保持 `'update'`，因此天然不计入 |
| 资源申请通过、拒绝               | [resource-apply/service.ts](../../app/api/admin/resource-apply/service.ts) 已在事务内各写一条，type 是通用的 `'approve'`、`'decline'`，与创作者审核等动作无法区分                                               | 改成 `resource_apply_approve`、`resource_apply_decline`，仍各一条                                                                                 |
| 反馈、举报                       | —                                                                                                                                                                                                               | 本批只读，不产生待审事项动作，不计入                                                                                                                |

计数查询：`admin_log` 中 `user_id = 当前管理员`、`当日起点 <= created < 下一日起点`、`type` 属于上述三个专用取值的行数。按动作逐次计数而不是按对象去重——要求修改后重提再通过是两次真实处理，应记两次；一次动作只写一条专用行，所以不会被附属日志膨胀，也不需要把对象 ID 结构化解析出来（对象 ID 仍如现状写在 content 文本里，仅供追溯）。不新增表、不新增字段。

三个新 type 取值要在 `constants/admin.ts` 的 `ADMIN_LOG_TYPE_MAP`（第 16 行，现有键为 create、delete、approve、decline、update、grant）补中文标签，否则日志页标签为空。日志服务只做分页、日志卡片的颜色映射有回落，所以除补标签外不需要别的改动，也没有按 type 的白名单要挂。历史行保留旧取值：上线前的资源申请动作与投稿审核日志不计入当日计数，属一次性、可解释的边界。

自然日起点复用既有 [getShanghaiQuotaWindows](../../app/api/patch/resource/download/access/timeWindow.ts) 的 `dailyStart`，由调用方传入 `now`：不新建日期库、不依赖数据库时区、不改系统时钟。

## 5 接口、校验、权限、幂等

### 5.1 命名与响应约定

界面在 `/dashboard`（D2-1 已定），核心管理接口沿用 `/api/admin/*`，不新建命名空间：接口与界面库无关，[middleware.ts](../../middleware.ts) 对 `/api` 的匹配与排除项不必改。[middleware/\_csrf.ts](../../middleware/_csrf.ts) 只校验 POST、PUT、PATCH、DELETE，新增的 GET 接口不需要排除项。新接口一律 `Cache-Control: private, no-store`。错误契约沿用现状：管理接口以 HTTP 200 返回中文字符串表示失败，投稿审核状态已变时返回 409；控制台请求层按「响应是字符串即失败」处理，待审队列收到 409 只刷新该项；历史视图按第 6.5 节重读当前筛选列表。

请求复用 [kunFetch.ts](../../utils/kunFetch.ts)，调用路径按其规则省略自动添加的 `/api`。现有 `kunFetchPost` 会将非 2xx 的字符串响应直接返回；投稿界面沿用 [AdminSubmissionDetail.tsx](../../components/admin/submission/AdminSubmissionDetail.tsx) 对 `PATCH_SUBMISSION_REVIEW_STATE_CHANGED_MESSAGE` 的识别方式，提示冲突并刷新当前项。HTTP 409 由接口用例核实；其他字符串失败与网络异常分别展示，不为后台另建请求层。

### 5.2 `GET /api/admin/inbox`（已实现 `app/api/admin/inbox/route.ts` 与 `service.ts`）

- 门槛 `role >= 3`。
- 校验：使用 `validations/inbox.ts` 的 `adminInboxQuerySchema`，`kinds` 以单个逗号分隔查询参数传递（例如 `kinds=submission,report`），由本接口 schema 转换并校验为来源枚举数组，省略时默认全部；适配既有查询工具的单值编码，不修改全局解析器。`limitPerKind` 默认与上限 50（与既有后台投稿列表的 limit 上限一致，见 [validations/patchSubmission.ts](../../validations/patchSubmission.ts) 第 250 行；四类候选都由待审事项服务自行查询，不受既有分页 schema 的约束）。
- 取数：按 4.2 对每个来源取一页候选，并用同一谓词单独计数；实际总数大于候选数时该来源标 `truncated`，界面显示「该来源还有更多，先处理这些」。因为每个来源都最老优先，全局最老项必然落在某来源候选内；筛选到单一来源时该来源最老项同样在首屏。
- 归一化字段：`key`、`kind`、`id`、`title`、`subtitle`、`actor`、`waitingFrom`、`waitingSeconds`（服务端计算）、`targetHref`、`badges`、`readOnly`、`payload`（沿用各来源既有行结构，不新造类型）。
- 排序在服务端完成：默认 `waitingSeconds` 降序，可切换按来源分组；界面不重排。

2026-09-10 用户已批准并实现：`search` 先在所选来源的全部待审记录中匹配，再按等待起点取各来源最老候选；不是只在当前候选窗口中筛选。关键词 trim 后最长 300 字符，空值表示全部待审记录。使用不区分大小写的包含匹配，字段如下；合法正整数还会与各来源主键精确匹配。

| 来源 | 搜索字段 |
| ---- | -------- |
| 投稿 | `name`、作者 `user.name` |
| 资源申请 | `name`、`note`、`patch.name`、`user.name` |
| 反馈 | `content`、`sender.name` |
| 举报 | `reason`、`patch.name`、`sender.name`、`reported_user.name` |

列表返回 `{ items, totals, truncated }`，`totals` 使用同一筛选和搜索谓词，未选来源为 0；`truncated` 仅描述本次匹配结果。侧栏 counts 始终查询四来源全部待审记录，不随搜索或来源筛选改变。共享结构为 `types/api/inbox.ts`；单项返回 `{ state: 'pending' | 'processed', item }` 或 `{ state: 'missing', item: null }`，所有时间字段使用 ISO 字符串。举报 payload 的 `pendingForTarget` 表示同目标其他待审举报数，目标已删除时为 0；队列仍逐条返回举报。


**深链接取项**（已实现 `app/api/admin/inbox/item/route.ts`）：按 `kind` 与 `id` 单条读取，鉴权与列表一致（`role >= 3`），返回同一套归一化结构外加 `state`（仍待处理、已被处理、已不存在）。用途是候选窗口之外的深链接与他人处理后的刷新；选中项不插入列表排序，也不改变截断口径；已处理与不存在分别给出对应提示。投稿的完整详情仍走 5.4。不为此新建通用详情路由框架。

### 5.3 `GET /api/admin/inbox/counts`（已实现）

门槛 `role >= 3`。返回四来源的精确待处理数（与列表同谓词，不复用会把已处理反馈算进去的既有 total）与今日已处理数。界面每 60 秒轮询，动作成功后立即刷新。

### 5.4 `GET /api/admin/patch-submission/[id]`（已实现）

完整详情由既有 `getAdminPatchSubmission(id, reviewerRole)` 生成（[service.ts](../../app/api/admin/patch-submission/service.ts) 第 147 行起，内部校验 `PATCH_SUBMISSION_REVIEW_MIN_ROLE`）。route 层显式校验登录与 `role >= 3` 后调用同一服务，返回既有结构。会社诊断在 `preview.companyDiagnostics` 之下（[AdminSubmissionDetail.tsx](../../components/admin/submission/AdminSubmissionDetail.tsx) 第 115 行），控制台读取路径必须一致。

实现位置为既有 [patch-submission/[action]/route.ts](../../app/api/admin/patch-submission/[action]/route.ts)：新增 `GET`，将 `params.action` 按合法投稿 ID 严格校验并单独鉴权；`POST` 保留现有四个动作的白名单、请求体和权限。两个 HTTP 方法共用这个动态段文件，详情 URL 仍为 `/api/admin/patch-submission/{id}`。

### 5.5 处理接口与门槛

| 动作                                             | 现状门槛                                                                                                                                        | 本模块                                 |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| 投稿通过、要求修改、驳回、判违规                 | 服务内审核角色常量；自审需超级管理员显式 override                                                                                               | 不变，直接复用                         |
| 资源申请通过、拒绝                               | `role >= 3`                                                                                                                                     | 不变，直接复用                         |
| 反馈处理、举报处理                               | `role < 4` 拒绝（[feedback/handle](../../app/api/admin/feedback/handle/route.ts)、[report/handle](../../app/api/admin/report/handle/route.ts)） | 不动；待审事项不调用，处理入口链接旧后台 |
| 创作者申请、统计、日志、设置、邮件、封禁、删条目 | `role >= 4`                                                                                                                                     | 不动                                   |

读取面已由基线 2.2 与总计划 M01-3 定案：四来源在待审事项里对 `role >= 3` 可读，含旧反馈与旧举报的标题与详情；这两类的写权限维持现状的 `role >= 4`，不下调，也不需要再走一次批准。由此产生的角色预期：`role >= 3` 能看到这两类并跳到旧后台，但旧后台里的处理动作会返回既有的「本页面仅超级管理员可访问」，所以跳转入口上直接标注「处理需超级管理员」，不承诺旧后台的每个链接对 `role 3` 都能完成操作。

其余既存门槛不一致（评论管理两处、资源管理接口与页面动作、发放萌萌点与同文件其他方法）在此登记；核心交付不迁移这些页面，按模块 06、09 的契约随实际迁移落实；评价举报读取门槛已按第 6.8 节的旧页回归修复对齐。第 6.9 节的早迁项由承接任务同步处理权限，09 核对完成情况。下调任何门槛都是真实权限变更，回退时界面与接口必须一起退。

### 5.6 幂等与并发

投稿四个动作使用 `claimPending`（带 `status: 'pending'` 条件的 `updateMany`，0 行即报状态已变）。通过、驳回、违规路径已将抢占移至 `releaseMoemoepoint`／`earnMoemoepoint`／`forfeitMoemoepoint` 之前；通过保持先 `publishCore`、再 `claimPending`、后结算的同事务顺序，抢占失败时发布写入随事务回滚。要求修改继续使用原抢占路径。此调整避免带押金的并发落败方先触发已结算 reservation 错误，保留原 409 与中文冲突提示。

实施前资源申请：`approvePatchResource` 事务外读 status、事务内无条件 `update`；`declinePatchResource` 事务内无条件 `delete`；两者的 `createMessage` 都传了事务客户端。并发下通过会重复发通知并重复写日志，拒绝的落后方会因行已不存在抛异常。对照事实：`handleFeedback` 的 `createMessage` 未传事务客户端，其通知本就在事务之外；本批不改反馈与举报服务，故不对它们声明同事务语义。

已实现两处条件写，均在既有事务内、对外契约不变；真实并发实验仍待验收：`approvePatchResource` 用带 `status: 2` 条件的 `updateMany` 抢占，更新 0 行则不发通知、不写日志，返回既有提示「当前资源状态无需审核」；`declinePatchResource` 用带 `status: 2` 条件的 `deleteMany` 抢占，删除 0 行同上，S3 清理仍在事务提交后执行且只有胜出方执行。通过与拒绝互相竞争时由同一个 `status = 2` 条件决出胜负，落败方按各自的既有提示返回，不会出现「先删后通过」或两条通知。原则：幂等靠本业务的原子状态转换，不靠先读后写，不靠前端或 Redis 锁；同一动作只由胜出方发送一次系统通知。界面的在途请求保护只是体验优化，不作为正确性依据。

### 5.7 中间件、鉴权与爬虫

- [middleware.ts](../../middleware.ts) 的 matcher 已包含 `/dashboard/:path*` 与 `/preview/:path*`。
- [middleware/auth.ts](../../middleware/auth.ts) 的 `protectedPaths` 已包含 `/admin`、`/dashboard`、`/preview`、`/user`、`/comment`、`/edit`，匹配完整路径或带 `/` 边界的子路径。
- 中间件只判登录；角色门槛在控制台段布局、预览页与每个接口各执行一次。
- [app/robots.ts](../../app/robots.ts) 的 `DISALLOW_PATHS` 已加入 `/dashboard` 与 `/preview`。
- 预览采用同源 iframe 内嵌：[next.config.ts](../../next.config.ts) 已按 D2-5 用 `headers()` 只对 `/preview/:path*` 返回 `Content-Security-Policy: frame-ancestors 'self'` 的最小配置；预览页含可点链接，不能以「没有动作」为由省略同源限制。不引入其他安全头或新机制。

## 6 前后台界面及文件

### 6.1 路由重排（M01-2）

实施前 `app/` 根文件 7 个：actions.ts、error.tsx、layout.tsx、metadata.ts、page.tsx、providers.tsx、robots.ts；顶层页面段 22 个：[id]、admin、apply、auth、comment、company、doc、edit、friend-link、login、message、moemoepoint、otomegame、ranking、redirect、register、resource、search、settings、submission、tag、user。

已完成目录移动：`robots.ts` 与 `api/` 留在根（元数据路由与路由处理器不需要布局），其余 6 个根文件与 22 个段整体移入 `app/(site)/`，保证 `./providers`、`./metadata`、`./actions` 等相对导入不变；新增 `app/(site)/not-found.tsx` 与预览路由；新增 `app/(dashboard)/layout.tsx`（第二根布局）、`app/(dashboard)/error.tsx`、`app/(dashboard)/dashboard/layout.tsx`（鉴权与外壳）、`app/(dashboard)/dashboard/page.tsx`（统计首页）与 `app/(dashboard)/dashboard/inbox/page.tsx`（待审事项）。公开页面仅做路由组移动，公开 URL 不变；控制台旧查询深链接按第 6.10 节兼容。

### 6.2 两个根布局的内容对照

| 内容                                                   | `(site)`                               | `(dashboard)`                                                  |
| ------------------------------------------------------ | -------------------------------------- | -------------------------------------------------------------- |
| 样式入口                                               | `styles/index.css`                     | `styles/dashboard.css`                               |
| HeroUI Provider 与 HeroUI Toast                        | 是                                     | 否                                                             |
| `next-themes`（`attribute="class"`）                   | 是                                     | 是，沿用同一默认存储键，深浅色前后台共享                       |
| 进度条 Provider、`react-hot-toast`                     | 是                                     | 是（都不依赖 HeroUI，可直接复用）                              |
| 站点主题脚本、`data-kun-theme`、未读消息轮询、分析脚本 | 是                                     | 否                                                             |
| 顶栏、面包屑、页脚、回到顶部                           | 是（预览路径下不渲染，见 6.7）         | 否，控制台自带侧栏与标题栏                                     |
| viewport 与 metadata                                   | 现有 `kunViewport` 与现有 metadata     | 自带且允许缩放；标题为控制台，`noindex`                        |
| 字体                                                   | `index.css` 的全局 `!important` 字体栈 | 在 `dashboard.css` 对 `body` 声明同一字体栈，不用 `!important` |

### 6.3 样式隔离与依赖（D3）

已新增 [styles/dashboard.css](../../styles/dashboard.css)：以 `@import 'tailwindcss' source(none)` 开头，导入 `tw-animate-css` 与 `shadcn/tailwind.css`，显式扫描 `../app/(dashboard)`、`../components/dashboard`、`../hooks/dashboard`；深色变体与前台一致，随后是 shadcn 令牌与基础层。[styles/tailwind.css](../../styles/tailwind.css) 排除这三个 dashboard 目录，继续加载 HeroUI 主题。

[components.json](../../components.json) 使用 `new-york`、`neutral`、CSS variables 与 lucide，样式指向 `styles/dashboard.css`，别名指向 dashboard 组件、工具和 hooks。基础组件来自官方 shadcn/ui 组件源码，MIT 声明保留在 [components/dashboard/ui/LICENSE](../../components/dashboard/ui/LICENSE)。

D3 实际清单以 [package.json](../../package.json) 和 [pnpm-lock.yaml](../../pnpm-lock.yaml) 为准：`shadcn 4.21.0`、`radix-ui 1.6.7`、`class-variance-authority 0.7.1`、`tw-animate-css 1.4.0`、`react-resizable-panels 4.12.4`；复用锁定的 `Tailwind CSS 4.1.11`、`lucide-react 0.526.0`、`clsx` 和 `tailwind-merge`，框架为 `Next.js 15.5.18`、`React 19.2.4`。控制台请求沿用 `kunFetch`，没有新增数据层。

两份样式的导入链与扫描范围已经拆开；类名或令牌重名不能证明产物隔离，E01-01 仍须用待发布构建验证各自 CSS、脚本、布局与预览。当前源码与依赖清单完成不代表该实验通过。

### 6.4 控制台外壳

侧栏：统计总览（`/dashboard`）、待审事项（`/dashboard/inbox`，带待处理总数）、四个来源筛选（各带计数）、「旧后台」分组（逐条链接未迁移页面并标注「旧」，其中需超级管理员的页面另标注「处理需超级管理员」）、返回前台、深浅色切换、当前账号与退出；桌面固定，移动端抽屉。标题栏按页面显示标题、计数刷新与今日已处理；待审事项另有搜索、列表刷新与键盘帮助，帮助按钮仅在桌面显示。手机待审事项页的标题栏和内容共用滚动容器，列表的来源筛选与搜索区域到顶后固定，详情的「返回列表」行到顶后固定；上滚自然露出完整顶栏，在返回区域下拉可平滑回顶（减少动态效果偏好下直接回顶）。桌面继续使用固定标题栏与分别滚动的双栏。统计首页按第 6.10 节展示，待审事项主区左列表右详情，桌面可拖分栏，移动端单列切换；筛选与选中项进 URL，可深链接、可刷新。入口切换：顶栏管理后台导航项与个人页跳转改指控制台；跨根布局是整页加载，现有 `router.push` 仍可用；旧后台地址在过渡期保持可直接访问。

### 6.5 待审事项

列表行：来源图标、标题、副标题、等待时长、徽标（只读来源标注「旧，去旧后台处理」）。默认按等待时长降序。

仅选择条目来源时提供旧列表的七种状态：待审核、草稿、要求修改、已驳回、已发布、违规关闭、已删除。待审核继续读取 `/api/admin/inbox`；其他状态复用 `/api/admin/patch-submission` 的 `status`、`query`、`page` 与每页 50 条，不新增历史接口。URL 使用 `submissionStatus`、`submissionPage`；换状态或提交历史搜索时回到第一页并清除选中项，切回多来源时移除历史参数。历史搜索沿用旧接口的 107 字符上限，输入、有效查询与生成的地址一致；待审搜索仍为 300 字符。越界页或缩水后的空尾页沿用旧列表回退规则，不把无效页显示成无记录。

历史列表展示真实状态与上海时区时间：草稿显示更新时间，其他状态优先显示审核时间、缺失时显示更新时间；其总数只表示当前状态的匹配量，不写入侧栏待审计数。选中记录继续使用既有单项与完整详情接口，按当前真实状态启用审核动作。待审队列的冲突仍只刷新该项；历史状态变化后重读当前筛选列表，避免保留已不符合状态或搜索条件的记录。切换历史状态、搜索或分页后，旧请求不得覆盖新列表或详情；成功审核即使晚返回到历史视图，也须刷新全局待审计数。

| 来源         | 详情                                                                                 | 动作                                                                                                                 |
| ------------ | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| 投稿         | 详情接口字段、押金与角色、VNDB 重复、会社诊断四节、投稿页面预览                      | 通过、要求修改、驳回、判违规均需确认；后三项需理由。自审按现状显示超级管理员 override                    |
| 资源申请     | 名称、分区、类型、语言、平台、备注、全部链接与提取码、发布者与其资源数、资源卡片预览 | 通过、拒绝均需确认，拒绝需理由、「编辑后通过」链接旧后台（资源编辑表单是前台 HeroUI 组件，模块 06 重做前不复刻） |
| 反馈（只读） | 全文、反馈人、关联链接                                                               | 「在旧后台处理」链接，标注处理需超级管理员                                                                           |
| 举报（只读） | 举报理由、被举报内容、被举报人、所属条目、同目标其他待处理举报                       | 「在旧后台处理」链接，标注处理需超级管理员                                                                           |

键盘：上下选择、打开详情、正向动作和破坏性动作（均进入确认）、聚焦搜索、显示帮助；只读来源不绑定处理键；焦点在输入框、文本域或任一弹窗打开时，列表快捷键失效。鼠标和快捷键只打开同一确认框，单独确认后才调用原处理接口；取消/Esc 不写入，异步失败保留理由与重试，同步在途锁阻止重复提交，选择或数据版本变化后关闭旧确认。旧后台已有审核确认沿用，不再叠加一层。基线 3.7 的四条回复模板（已修复、需要截图、请参考指南、不在受理范围）对应的来源在本批为只读，故本批不落地模板库，随模块 03 落地。待审队列处理成功即移除该项并选中下一项，计数刷新；失败按接口返回的中文字符串提示，409 只刷新该项。历史视图按本节规则重读当前筛选列表。会社阻断型歧义存在时禁用通过，与现状一致，本模块不改判定与文案。

### 6.6 未匹配路由与错误边界

已在 `(site)` 新增 `not-found.tsx`。站点内主动触发的未找到、站点段内未匹配路径及「完全未匹配的顶层路径」由哪个根布局承接，仍须在仓库精确版本上实测。只有实测证明无法承接，才提请启用实验特性的全局兜底（D2-6），未经站长同意不在生产启用。控制台单独有 shadcn 实现的错误边界。

### 6.7 前台预览

已新增 `app/(site)/preview/submission/[id]/page.tsx`：服务端组件，未登录跳登录、非管理员跳首页，调用既有详情服务取预览数据，渲染现有投稿预览视图组件；位于前台根布局下，样式、Provider 与主题与前台完全相同。站点外壳隐藏：在 `constants/routes/matcher.ts`（现有一组 `isXxxPath` 判定函数）新增预览路径判定，让顶栏、面包屑返回空，外壳组件不渲染页脚与回到顶部；三者都是已有条件分支的客户端组件。控制台投稿详情用同源 iframe 嵌入，普通内嵌窗口按预览内容容器的实际高度收缩，上限保持 `max(70dvh, 560px)`，`sm` 起为 `max(70dvh, 640px)`；长内容在窗口内滚动，资源展开、收起与重新加载后重新测量。提供「放大预览」对话框及「在新标签打开」。放大视图沿用 shadcn Dialog 的关闭与焦点恢复；iframe 内的键盘事件不跨文档冒泡，关闭按钮始终可达。深浅色因共享主题存储键自动一致。

基线 7.3 同样把资源卡片预览列为「同理适用」，已一并新增 `app/(site)/preview/resource/[id]/page.tsx`：同一套鉴权与外壳隐藏结构，按资源 ID 读取一条待审资源（复用 4.2 的资源申请谓词，只取一条），复用前台现有卡片——[ResourceDownload.tsx](../../components/patch/resource/ResourceDownload.tsx) 导出 `ResourceDownload({ resource, preview })`，内部渲染同目录 `DownloadCard.tsx` 的 `ResourceDownloadCard`，并带有恢复计数的副作用与点赞按钮。这两个组件已增加只读 `preview` 展示参数，在预览下跳过恢复计数副作用、把点赞渲染成静态数字而不是可点按钮、链接与提取码直接来自管理员授权读取的预览数据，不对 pending 数据调用面向公开用户的揭示与下载接口；展开与展示类交互保留。不复刻卡片渲染逻辑，也不建第二套卡片框架。控制台的资源申请详情同样内嵌并提供独立打开。两个预览按 D2-5 共用前台根布局、路径判定与鉴权。

### 6.8 文件清单

已落地地基与接口、业务组件集成中：`app/(dashboard)/**`、`app/(site)/not-found.tsx`、`app/(site)/preview/submission/[id]/page.tsx`、`app/(site)/preview/resource/[id]/page.tsx`、`components/dashboard/**`、`lib/dashboard/utils.ts`（`components.json` 的 utils 别名指向它，首个组件即需要）、`styles/dashboard.css`、`components.json`、`app/api/admin/inbox/**`（含 `item`、`counts`）、`validations/inbox.ts`、`types/api/inbox.ts`。`hooks/dashboard` 已按基础组件需要加入 `use-mobile.ts`。

移动：`app/` 的 6 个根文件与 22 个页面段进入 `app/(site)/`。

投稿详情读取按第 5.4 节修改既有 `app/api/admin/patch-submission/[action]/route.ts`，原审核动作同文件保留。

修改：[middleware.ts](../../middleware.ts)、[middleware/auth.ts](../../middleware/auth.ts)、[app/robots.ts](../../app/robots.ts)、[styles/tailwind.css](../../styles/tailwind.css)、`constants/admin.ts`、`constants/top-bar.ts`、`constants/routes/matcher.ts`、顶栏与面包屑及外壳三处客户端组件、个人页跳转组件、[resource-apply/service.ts](../../app/api/admin/resource-apply/service.ts)、[review.ts](../../app/api/patch-submission/review.ts)（给 `writeAdminLog` 加可选 type 参数及四动作专用日志，并将通过、驳回、违规的 pending 抢占移至结算之前；通过仍先执行 `publishCore`）、[ResourceDownload.tsx](../../components/patch/resource/ResourceDownload.tsx) 与同目录 `DownloadCard.tsx`（只加只读预览参数，默认行为不变）、根布局测试的导入与 mock 路径；预览的同源内嵌另加 [next.config.ts](../../next.config.ts) 的 `headers()`。

旧后台增加 `admin/layout.tsx` 的返回新后台入口，并将 `admin/rating-report/actions.ts` 的读取门槛与既有 API 对齐到超级管理员，避免管理员进入后因拒权响应而报错。核心交付不动：`components/admin/**`、[feedback/service.ts](../../app/api/admin/feedback/service.ts)、[report/service.ts](../../app/api/admin/report/service.ts)、[get.ts](../../app/api/admin/resource-apply/get.ts)、创作者申请写接口、部署与发布脚本；统计读取按第 6.10 节补充。第 6.9 节选定的早迁页面按对应职责局部变更，不整批重写这些目录。

### 6.9 稳定管理页面早迁

开工与发布节奏见 [PM 版第 5.1 节](./01-admin-foundation-pm.md#51-稳定管理页面提前迁移2026-09-10-已确认)。控制台地基、待审事项和预览主流程基本完成，且样式隔离与旧后台回归通过后，可开始迁日志等稳定页面；用户管理及用户萌萌点明细已选定早迁；创作者申请如早迁则按 D1 使用独立页，其他页面逐项选定。沿用总计划第 4.10 节的路径表记录承接任务、目标与完成证据，不新增模块、调度框架或另一套迁移清单。

- **界面与业务边界**：新页面使用 `components/dashboard` 与控制台样式，旧页面继续在 `(site)/admin` 使用原布局；不把旧后台整页组件或样式入口导入控制台。复用现有服务、类型与无界面依赖的工具，同一业务只保留一套处理逻辑。新界面覆盖常规操作和错误分支后才切入口；旧后台只做必要维护。
- **已选定的用户管理早迁**：`/admin/user` → `/dashboard/user`，`/admin/user/[id]/moemoepoint` → `/dashboard/user/[id]/moemoepoint`。迁移用户查询、既有编辑/封禁、删除、关闭两步验证、发放萌萌点及明细查看。复用 [user 接口](../../app/api/admin/user/route.ts)、[关闭两步验证接口](../../app/api/admin/user/2fa/disable/route.ts) 和现有账本 HTTP 查询接口 `/api/user/[id]/moemoepoint/ledger`，不另造用户或账务服务。列表、更新与删除、关闭两步验证维持 `role >= 4`；发放萌萌点维持 `role >= 3`；账本沿用 [access.ts](../../app/api/moemoepoint/access.ts) 的本人或管理员规则，控制台入口仍要求管理员。用户列表页单独执行超级管理员鉴权，不能用用户目录统一的 `role >= 4` 布局拦住原本可访问明细的管理员。验证分页/搜索、各既有动作、role 3/4 的边界、明细筛选与旧链接，再按本节流程发布和逐页退役。
- **权限与必要修复**：按 [09 实施计划](./09-metrics-and-retirement.md)第 5.3、5.4 节落实选中页面的权限及必要修复，相关界面、接口和回退一起核对。创作者早迁须带条件审核、通知去重与角色保护，并接好专用日志标签和今日计数；不为提前展示预建 09 的指标体系。早于 06 时不读取或写入尚未存在的 `admin_log.queue_entered_at`，该字段上线后的创作者写入由 09 接齐，时长从真实写入起算。
- **逐页验收与退役**：上线前验证主要动作、读取与写入权限、空态/失败、样式隔离、带查询参数及详情 ID 的旧链接、返回控制台导航；创作者同时执行 09 测试方案中该队列的并发和角色保护用例。布局与过渡证据并入现有实验记录，页面专属用例随承接任务记录，不新增实验编号或把计划标为已执行。页面发布并稳定使用后，按 09 第 6.3、6.4 节逐页重定向并清理无引用的旧界面，复用的 API/service 保留，未迁移页面不受影响。

早迁按独立页面交付，可随 01 首批发布，也可后续发布；合入首批的页面必须完成上述验收，未完成的继续保留旧页面，不阻塞 01 核心上线与 02 启动。反馈、举报的流程替换留给 03，资源管理留给 06，09 对已迁移页面只补剩余指标接线与最终核对。

### 6.10 统计首页（2026-09-11 已确认）

`/dashboard` 展示统计总览，`/dashboard/inbox` 保留完整待审事项。首页收到 `kinds`、`kind`、`id`、`search` 或 `order` 任一旧待审事项参数时，转向待审事项并保留全部参数及重复键值；新导航和各来源链接直接指向待审事项新地址。旧 `/admin` 统计页在发布并稳定使用前保留，不提前退役。

- **接口与权限**：复用 `/api/admin/stats?days=1..60` 和 `/api/admin/stats/sum`，两者均保持 `role >= 4` 并对成功与错误响应设置 `private, no-store`。`days` 只接受 1～60 整数。普通管理员首页仅使用已授权的 `DashboardShell` counts，不请求全量/增长统计及创作者申请数。
- **旧口径保留**：`SumData` 的用户、条目、游戏资源、补丁资源、评论五字段不变；资源总量包含各状态。`OverviewData` 仍为滚动窗口内新增用户、最近登录用户、条目、资源、评论，默认 1 天，可选任意 1～60 天整数；最近登录人数不称为 DAU，不将滚动窗口换为上海自然日，也不把现存记录数称为完整历史累计。
- **新增计数**：`DashboardStatsSumData extends SumData` 增加 `ratingCount`（现存全部评分）、`submissionCount`（现存全部投稿）、`creatorCount`（当前 `user.role = 2`）、`pendingCreatorApplyCount`（`user_message.type = 'apply'`、`recipient_id IS NULL`、`sender_id IS NOT NULL`、`status IN (0, 1)`）。创作者申请的未读/已读均属于尚未完成，不复用包含已处理项的旧列表 total；本轮仅补计数与旧页链接，不改旧审核流程。
- **待办与刷新**：四类待办、其合计和本人今日已处理直接复用 `DashboardShell` 的 counts、60 秒轮询及刷新状态；合计标明「待审事项未处理」，独立创作者申请不混入。统计按进入页面和手动刷新读取，不增加第二套计数轮询；尚未读取和错误不显示成 0，快速修改窗口时旧响应不得覆盖新窗口。
- **界面与验收**：以官方 shadcn `dashboard-01` 的概览卡片、间距与响应式布局为参考，在已有外壳内组合现有组件，不导入旧 HeroUI 统计组件。统计、待办、窗口、错误与权限状态分别清楚展示；没有真实时间序列的数据不画趋势图。相关路由、角色、计数、查询竞态和桌面/移动端深浅色验收纳入 E01-01/02/03，不新增实验或指标平台。09 继续负责后续运营指标及历史快照。

## 7 定时任务

无。各来源计数、今日已处理与首页统计在请求时聚合，不引入 cron 或快照表；运营指标的历史快照属于模块 09。

## 8 迁移与回填

- 数据迁移：无。不执行 `prisma migrate`、`db push` 或任何回填 SQL。
- 路由重排用 `git mv` 完成，公开 URL 不变，提交前逐条核对 6.1 清单。
- 行为变更三项：`admin_log` 出现三个新 type 取值（资源申请两处由通用 `approve`、`decline` 改为专用取值，投稿四个动作的主日志由 `update` 改为专用取值，诊断日志不变）；资源申请处理由先读后写改为条件更新与条件删除；旧反馈与旧举报的内容在待审事项内对 `role >= 3` 可读、写权限不变，依据基线 2.2 与总计划 M01-3，属已定范围而非待批准项。
- 回退边界：界面与路由可整体回退；权限口径回退必须界面与接口同时回退。已经写入的新类型日志行在旧代码下标签为空，退代码消不掉，只能补标签或接受。已按条件更新处理过的资源申请数据在旧实现下不会损坏，但会重新引入重复通知风险。
- 发布流程沿用仓库既有约定：备份与只读 preflight，无 schema 变更故不执行 sync，随后 postflight 与 schema guard、构建部署、冒烟验证。本模块只需证明没有 schema 漂移。文档阶段不运行上述任何命令。
- 文档与技能同步单独提交，不混业务代码：前端技能文件把「HeroUI v2 是强制设计系统」限定到前台路由组并加控制台条款；App Router 文档补两个根布局；组件分层表补 `components/dashboard/*`；主题色系文档的扫描命令排除控制台目录；开发指南样式清单加入 `styles/dashboard.css`。

## 9 测试与实验方案

### 9.1 自动化测试（实现期编写，作为对应实验的证据来源）

- E01-02 证据：待审事项列表、计数、单项取项接口的鉴权（未登录、role 1、role 2、role 3）、来源筛选、截断标记、排序；单项取项在候选窗口外、已处理、不存在三种情况下的 `state`；投稿详情接口鉴权；聚合服务在 mock 各来源查询下的归一化与排序；资源申请两个动作在「已被处理」时不发通知、不写日志、返回既有提示；中间件新前缀被识别为受保护路径。
- 前后端交接用例并入 E01-02：多来源参数编码与解析一致；投稿详情 GET 的 ID/鉴权校验和同文件原 POST 动作分别有效；投稿 409 响应经过既有请求工具后仍能触发单项刷新。
- E01-02 补充：投稿四动作与资源两动作逐项证明先确认后请求、取消/Esc 零写入、鼠标与快捷键一致、重复确认不重复发送、切项/版本变化不提交旧确认，原权限/冲突/理由校验不变。
- E01-01/03 补充：统计首页与 `/dashboard/inbox` 的直达、导航和原查询深链；role 3 不调用全量统计或创作者申请接口，role 4 保留旧指标与 1～60 天窗口，并展示新增计数；待办复用全量 counts，创作者申请不混入四来源合计；加载/失败不冒充 0，快速改窗口不被旧响应覆盖，移动端/深浅色/键盘可用。
- E01-03 证据：今日已处理在注入 `now` 与固定自然日边界下的逐次计数与跨日归零；专用 type 过滤掉会社诊断附属日志；「要求修改后重提、再由另一管理员通过」时前一位管理员当日计数不变。
- E01-01 证据：根布局测试导入路径更新后断言不变；生产构建通过。
- 因为改动 `styles/tailwind.css` 与根布局，按仓库测试约定跑全量测试并跑一次生产构建。不引入新的组件测试工具（D2-7）。

### 9.2 实验（核心流程已验证，旧页少量分支与上线验收待完成）

三个实验都在隔离环境进行：独立数据库、独立 Redis、独立构建产物，不连接生产、不读取生产数据。

**E01-01 布局、样式隔离、预览和 404（隔离构建的布局、预览与交互验证已完成）**

- 目的：证明拆成两个根布局后公开地址不变、两套样式互不混入、预览渲染前台真实样式、未匹配地址有兜底。
- 前提与隔离环境：仓库精确版本的生产构建在隔离环境运行；role 4、role 3、role 1 各一个账号；一条含图片、画廊与外链的 pending 投稿，以及一条待审资源申请（含多个链接与提取码）；预览承载方式已按 D2-5 定稿。
- 关键步骤：① 生产构建并记录每条路由实际加载的样式与脚本产物；② 按导入链与产物内容检查控制台样式产物是否含 HeroUI 插件产出、前台样式产物是否含控制台目录扫描结果，不以类名异同下结论；③ 依次访问首页、任一条目页、登录、用户页、旧后台任一页、控制台、两个预览页、明显不存在的顶层地址、站点段内不存在的地址；④ 前台跳控制台、控制台跳前台、控制台跳旧后台各一次；⑤ 在投稿详情内看内嵌预览并切换深浅色，再用独立标签打开同一预览；对资源申请详情重复一次，核对资源卡片是前台真实渲染，并在打开与交互期间记录网络请求、比对点赞与下载计数等相关数据行，确认预览不产生业务写入、不调用公开揭示与下载接口；两个预览各用未登录与 role 1 访问一次，确认被拒；另测同源可嵌入、跨源被拒；⑥ 移动端宽度重复③⑤并验证控制台允许缩放。
- 通过判据：所有原公开地址返回与改造前一致的页面且未新增重定向；两个方向的样式产物检查都干净；投稿与资源两个预览在内嵌与独立打开下都是前台真实样式且深浅色与控制台一致，未登录与 role 1 访问都被拒，且预览期间没有任何业务写入或对公开揭示、下载接口的调用；两类未匹配地址都得到可读兜底页而非空白或 500。
- 失败判据：任何公开地址失效、改变或需新增重定向维持；隔离只有截图证据；任一预览可被未登录或非管理员打开；预览触发点赞、揭示、恢复计数一类的业务写入或调用公开揭示与下载接口；以新增资源预览为由改动根布局方案（承载方式只能由 D2-5 决定）；未匹配顶层地址无法被任何根布局承接（此时才触发 D2-6，且需先取得站长同意）。
- 验证时间点：M01-2 完成后先执行具备条件的根布局、样式、公开路径、404 与往返导航检查，两个预览的剩余检查记为待执行，不将整项实验标为通过；M01-5 上线前用待发布构建完整执行本实验。
- 所需证据：构建产物路由与资源清单、逐地址状态码表、两个预览在内嵌与独立打开下的深浅色截图、预览期间的请求记录与相关数据行前后对照、样式产物检查的命令与输出。清理：销毁隔离构建产物与容器，不保留任何凭据。

**E01-02 待审事项最老项、筛选、权限与审核冲突（后端隔离场景与实际界面联调已完成）**

- 目的：证明最老事项不被排序或截断掩盖，筛选与深链接稳定，权限在界面与服务端一致，写动作在并发与过期下只产生一次结果。
- 前提与隔离环境：隔离数据库造数：四个来源的待处理量都超过候选上限（上限 50 时各造 60 条以上），每来源各埋一条明显最老记录；反馈来源另造若干 `status` 非 0 的历史行；举报来源造同一评论下 3 条待处理举报与一条评价类举报；资源申请造一条会被 NSFW 偏好过滤掉的记录；另造一条 `submitted_at` 为空而 `created` 最老的 pending 投稿（该列可为空，用于验证 `COALESCE` 排序）；账号覆盖 role 1、2、3、4。
- 关键步骤：① 默认排序下检查全局最老的一条是否位于首屏第一项，并核对四个来源的截断标记；② 逐来源筛选后检查该来源最老记录是否在首屏；③ 反馈来源核对返回集合与计数都不含已处理行；④ 把浏览者 NSFW 偏好设为最严格后重新取数，确认被过滤那条资源申请仍在队列；⑤ 深链接：对首屏项、候选窗口之外的项（如某来源第 60 条）、刚被另一会话处理掉的项、以及一个不存在的 ID 各取一次，新窗口打开并刷新，核对选中项一致、窗口外的项能正常打开、已处理与不存在各给出对应提示，且都不插队进列表排序；⑥ 用 role 1、role 2 访问控制台页面与四个新接口，再用 role 3 访问同样内容；role 3 从待审事项打开旧反馈与旧举报的旧后台入口，确认拿到既有的超级管理员权限提示而不是可处理界面，role 4 在同一入口可完成处理；⑦ 只读来源核对没有处理动作、旧后台入口可达，并在旧后台处理一条后刷新待审事项确认该项消失；⑧ 并发：在隔离库用测试层屏障或受控事务交错，除同一动作重复提交外，另制造相对动作竞争——同一条投稿的两个不同审核动作、同一条资源申请的通过与拒绝；再制造过期场景（先处理再用旧列表提交）；不使用两个浏览器窗口、不新建压测平台或生产延迟开关。
- 通过判据：全局最老项在首屏第一项（含 `submitted_at` 为空的历史行样本），各来源筛选后本来源最老项可达，截断标记正确；候选窗口外与已处理、不存在的深链接都返回正确状态且不插队；反馈无已处理项；NSFW 偏好不影响待审队列；role 1、2 在页面与接口都被拒，role 3 可读四来源、能处理投稿与资源申请、在旧后台得到既有权限提示；只读来源无写入路径；并发（含相对动作）与过期下业务对象只发生一次状态变更、系统通知不重复、落败方收到既有中文提示或 409 并刷新成功。
- 失败判据：最老项拿不到（含因 NULL 排序被挤出候选窗口）、候选窗口外的深链接打不开、越权可读或可处理、同一事项处理两次、用户收到重复通知、落败方看到未处理的异常页。
- 验证时间点：M01-3 后跑①至⑦，M01-4 后跑全部；此后任何处理服务或候选上限改动都要复跑①⑧。
- 所需证据：造数脚本或 SQL 摘要、接口请求与响应记录、动作前后的数据库行快照、通知表条数对比。清理：删除隔离库与造数，不在生产执行任何一步。

**E01-03 处理计数与旧后台过渡（计数与旧后台可达性已验证，真实上线复核待执行）**

- 目的：证明今日已处理只统计成功的待审事项动作、按动作逐次计数且不被后续状态覆盖、跨自然日正确，并证明未迁移能力在过渡期按既有角色可达。
- 前提与隔离环境：隔离数据库；自然日边界由既有上海时区工具计算，测试与实验都注入 `now`（或使用假时钟），不修改开发机或数据库时区。
- 关键步骤：① 当日成功完成投稿与资源申请各一次处理，核对计数为 2；② 制造一次失败动作（对已处理对象重复提交）与一次参数错误请求，核对计数不变；③ 执行与审核无关的后台操作（改设置、发放萌萌点、删除评论），以及一次会额外写多行会社诊断日志的投稿通过，核对计数只按实际动作增加、不随日志行数膨胀；④ 管理员 A 对一条投稿「要求修改」，作者重新提交，管理员 B 再通过它，核对 A 当日仍保留那一次、B 增加一次；⑤ 把注入时间推进到次日 00:10，核对计数归零且昨日动作不再计入；⑥ 用第二个管理员账号处理一件，核对两人计数互不串；⑦ 从控制台侧栏逐条打开未迁移的旧后台页面，按访问账号的既有角色核对：页面可达，逐页验证主要查看与处理流程，需要超级管理员的动作对 role 3 返回既有提示、对 role 4 可用；⑧ 从顶栏与个人页进入控制台，同时直接访问旧后台地址，确认两者都可用、样式各自完整并能明确返回控制台。
- 通过判据：①至⑥计数全部符合预期；⑦⑧按既有角色预期可达可用。
- 失败判据：计数等于后台日志条数、把失败或重复请求计入、把会社诊断附属日志计入、某位管理员已完成的处理因后续再审而消失、跨日不归零、两个管理员计数互相污染、任一未迁移页面在控制台没有入口或已不可用。
- 验证时间点：M01-4 之后、M01-5 上线前；上线后由站长在真实一轮审核中复核当日计数与实际处理件数一致。
- 所需证据：每步的计数返回、对应业务对象与日志行清单、时间推进前后的对照。清理：删除隔离库与造数。

## 10 实施任务与验收

2026-09-11 当前工作区已实现双根布局与扫描边界、四来源待审事项及审核专用日志、资源条件写与投稿结算前抢占、两路前台只读预览、用户管理早迁、统计首页与全部审核动作的二次确认。以下区分既有证据与本轮增量验证；模块 01 未完成生产上线验收。

待审事项的 `tests/unit/inbox-validation.test.ts`、`tests/unit/api/admin-inbox-service.test.ts`、`tests/unit/api/admin-inbox-routes.test.ts` 已通过 72 个模拟单元测试，覆盖查询、三态、权限、跨日计数及既有 POST 冲突契约。此证据未连接真实数据库、Redis 或 S3，不能替代第 9 节的 E01-01/02/03；投稿审核抢占顺序另有 `tests/unit/patch-submission-review.test.ts` 的 30 个模拟用例通过；真实 PostgreSQL / Redis 隔离环境的 20 个场景已通过，覆盖四来源查询、NULL 排序、窗口外搜索与单项状态、资源和带押金投稿的并发审核、作者撤回/删除交错、上海自然日计数及旧项写权限。S3、Cloudflare、IndexNow 等外部服务使用隔离替身。布局诊断构建已证实两套 CSS 分离、两路预览鉴权与同源嵌入限制；实际控制台的隔离副本已通过 TypeScript 检查、235 个文件共 1948 个单元测试，以及 `pnpm build --no-lint`（含 postbuild）；实际待审事项的 15 个浏览器场景已通过，覆盖四种投稿动作、两种资源动作、只读来源、深链接与错误恢复；桌面、平板、移动端的深浅色检查及侧栏焦点回归已完成。两路预览的内嵌与独立打开、鉴权、同源限制、外链与折叠交互已核对，相关业务数据未写入。旧后台 17 个页面按 role 3/4 完成 34 次访问与可执行的查看、表单打开/取消回归；创作者申请与表情包后续补齐非空隔离样本，32 个桌面/手机权限、打开与取消步骤通过，数据库快照一致、零写请求、未复现 React 恢复性报错；该补验不代表旧后台所有实际写动作均已执行。用户管理与账本已按第 6.9 节开始早迁，其独立界面用例已通过，见下段。该批初验使用跳过 lint 的构建；后续标准检查的结果见本节当前收尾记录。

用户管理早迁的 148 个新增单元用例覆盖列表、编辑、发点/删除、账本与页面权限；10 个实际浏览器场景在 31 个新建隔离假用户上验证了查询/分页、角色边界、编辑与会话撤销、关闭两步验证、错误恢复、发点丢响应后的原请求重放及删除。重放只产生 1 条账本、1 条通知和 1 条发放日志。列表与弹窗按桌面、平板、移动端的深浅色完成检查；发点长理由的未知结果弹窗已补高度上限和内部滚动，并完成针对性回测。负余额显示通过只读响应替身及单元测试验证。该批新控制台各前端包均在原 K3 作者会话完成三轮互审；该批工作区类型检查及修改范围 lint 通过，尚未部署。

本轮统计首页与全动作确认：全量测试共 239 个文件、2015 个用例通过（隔离副本初次缺少 Git 版本信息导致 3 项失败，补齐只读 Git 元数据后单独重跑通过）；TypeScript 与隔离生产构建 `pnpm build --no-lint`（含 postbuild）通过。真实 Next.js 与隔离 PostgreSQL / Redis 的 9 个只读 HTTP 场景核对了统计字段、零值、1～60 天整数窗口、角色权限及私有禁止缓存响应。浏览器验证覆盖六个审核动作和两组路由兼容场景，确认鼠标/快捷键均先弹窗、取消/Esc 零请求、关闭后焦点恢复、弹窗内快捷键不穿透、最终确认只发一次请求；审核写请求被拦截，此证据不替代既有事务、通知与结算实验。统计页另通过 8 组桌面/平板/手机、深浅色与角色组合，普通管理员不请求完整统计，四来源合计不含创作者申请，手机窗口查询无溢出。浏览器非零展示数据来自只读响应替身，真实接口证据单独记录；页面使用官方 shadcn `dashboard-01` 的卡片与响应式组合。

最终修订后，24 个改动代码/测试文件的 lint 无错误或警告，36 个统计/首页用例及 46 个文档契约用例通过，155 条相对链接目标可达；最终生产构建与 postbuild 再次通过，并用该构建复核两种角色的首页、两组待审事项导航及上述 9 个真实只读 HTTP 场景。Astra 与原 Luna max 后端作者、各原 K3 前端作者会话完成三轮互审，文档也完成三轮审阅，无未解决的本轮代码或文档意见。旧后台实际写入的覆盖边界见本节记录；当前候选状态以本节最终验收结论为准。

| 任务                 | 交付物                                                                                                           | 验收                                                                                                                                       |
| -------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| M01-1 冻结设计       | 第 12 节决定已记录；补齐旧后台路由清点表与逐动作权限表                                                           | 按已定四来源、旧项只读、前台预览与确认策略核对全部路径。用户管理按 D2-4 单独早迁；D2-6 等 E01-01，D3 在 M01-2 记录，不据此反复等待设计确认 |
| M01-2 路由与样式隔离 | 6.1 的移动执行完毕；两个根布局；`styles/dashboard.css` 与双向扫描边界；`components.json` 与实测依赖清单          | 全量测试与生产构建通过；E01-01 中已具备条件的检查通过，预览等剩余项在 M01-5 完整验收                                                       |
| M01-3 待审事项读取     | 待审事项列表、计数、单项取项、投稿详情四个接口；四来源候选与归一化；候选按 4.2 第 4 条的 `COALESCE` 排序           | 排序、截断、空态、筛选、候选窗口内外的深链接、刷新正确；管理员可读、普通用户不可；E01-02 的①至⑦通过                                        |
| M01-4 处理与权限     | 投稿与资源申请动作接入；5.6 的两处条件更新；4.3 的专用日志 type 与标签；键盘与确认                               | 重复提交与相对动作并发只产生一次结果与一次通知；自审 override、状态冲突、会社歧义与现状一致；E01-02 全部通过、E01-03 的①至⑥通过            |
| M01-5 预览与上线     | 投稿页面与资源卡片两个预览路由与外壳隐藏；内嵌与独立打开；入口切换（含「处理需超级管理员」标注）；旧后台入口分组 | 前台真实样式、深浅色、移动端与键盘验证通过；E01-03 的⑦⑧通过；隔离环境用待发布构建复跑 E01-01；上线后站长完成一轮真实审核                   |

上线批次最小范围沿用总计划 3.3 节。上线前必须满足：投稿与资源申请可在待审事项完成处理，旧反馈与旧举报可见且有旧后台入口，未迁移能力有入口，权限、并发与计数三项有实验证据，文档与技能同步完成。

此前标准检查记录（2026-09-11）：确认弹窗退出动画期间的动作文案已修复，92 个相关用例通过。标准 lint 的 239 个错误已通过格式修正及少量行为等价修正清零，135 个既有警告未增加；Astra 已复审。用户本轮界面修订前的隔离候选通过完整的 `pnpm build`（包含 lint、类型检查和 postbuild）、239 个文件共 2027 个测试及类型检查。该结果只对应修订前候选，后续变更仍须按影响重测。

上线前用户验收已于 2026-09-11 通过，六项界面修订见 [PM 版第 7.1 节](./01-admin-foundation-pm.md#71-用户验收修订2026-09-11)。条目历史状态复用已有 `GET /api/admin/patch-submission` 的状态、搜索及分页，默认待审队列继续使用既有聚合接口；历史总数与侧栏待审计数分开。用户列表复用现有 `user.moemoepoint` 总额，不改变可用余额或账务写入规则。本轮六项修订已完成，按已批准的小修改规则完成作者修订与 Astra 复审；站长明确确认「可以，当前体验验收通过」。正式发布按总计划第 7、8 节执行；模块 02 实际开发仍须等待 01 上线。

前一轮候选的验证：241 个测试文件共 2083 个用例通过，类型检查通过，完整 `pnpm build`（含 lint、类型检查与 postbuild）通过；lint 为 0 错误、135 个既有警告。历史列表相关 105 个用例覆盖分页回退、107 字符查询、状态/搜索/分页切换、迟到结果隔离及原待审队列回归。真实 Next 候选在 390/1440 宽度下完成 11 组只读流程，统计、七状态控件、两种放大预览、手机阅读导航及用户列表/明细通过；0 页面错误、0 业务写请求、0 下载请求，业务表快照一致。六种历史状态的真实 GET 返回空记录后，用只读夹具验证真实组件展示；未将其视为真实历史数据持久化验收。当前开发库已按单独授权新增标记样本，四类待审各 2 条并补齐其他六种条目状态，供站长体验复验。全部证据汇总于本机 `/private/tmp/otoame-m01-release-20260911/acceptance.md`。

手机自然滚动增量验证（2026-09-11）：标题栏与手机列表、详情共用滚动容器，分别固定列表工具栏和详情返回行；手机隐藏快捷键帮助按钮。5 个测试文件、157 个相关用例及类型检查通过，本次改动文件的 ESLint 检查通过。真实 Next 开发页面在 390×844/640、767/768 与 1440 宽度下完成 15 项浏览器触摸模拟检查，覆盖连续滚动与吸附位置、下拉回顶、按钮显示断点、桌面独立分栏和跨页面滚动隔离；无页面错误或业务写请求。认证、GET 数据与预览文档使用明确标记的夹具，未连接业务数据库；站长已在本轮确认当前体验验收通过。此前 hydration 告警已由站长确认暂不再出现，独立开发环境未复现。该次增量验证未重做生产构建，当前完整候选的检查另记如下。证据见本机 `/private/tmp/otoame-m01-mobile-via-20260911/acceptance.md`。

**最终候选验收（2026-09-11）：功能、上线前用户验收与本地发布检查完成，已达到上线候选水平，无重大代码阻断；恢复性告警作为非阻断观察保留。** 站长明确确认当前体验验收通过；本轮代码在验证期间保持不变，1647 个非 Markdown 文件在工作区和构建副本中的 SHA-256 均与快照一致。 候选源码提交为 `7fd01cfd`，前置构建 lint 修正为 `ea2beddb`。

| 检查 | 最终结果 |
| ---- | -------- |
| 全量与改动测试 | 全量 241 个文件、2083 个用例；改动范围 111 个文件、1274 个用例，全部通过。为遵守不读 env 的边界，使用继承仓库配置且仅设置 `envDir: false` 的临时 Vitest 配置执行等价命令。 |
| 类型与文档 | `pnpm typecheck`、46 个文档契约用例及相对链接检查通过；代码与文档、技能分开提交。 |
| 正常生产构建 | 隔离副本执行 `pnpm build`，包含 lint、类型检查、sitemap 与 postbuild；未跳过检查。产物 `BUILD_ID = 5PRZy4VO2snuOapsJgJcN`；lint 为 0 错误、135 个既有警告。 |
| 生产产物回归 | 8 组合并功能场景通过：真实匿名与 role 1/3/4 权限、公开页面与 404、统计与用户页、新旧后台及跨根样式、两路预览、390/1440 宽度资源高度与放大关闭。投稿封面与画廊图片使用明确标记的本地读取夹具，接口、鉴权和预览渲染使用真实实现；预览未触发业务写入、揭示或下载请求；公共条目页自动产生的 `POST /api/patch/views` 已拦截，业务写请求实际传输为零，36 张表摘要不变。 |
| E01-01 完整补验 | 同一产物另完成 8 项功能/产物检查：390/1440 下两路预览的深浅色、内嵌与独立标签、手机缩放，移动公开路径与顶层/段内 404，四方向真实入口导航，CSP `frame-ancestors 'self'` 的实际跨源拒绝，以及 99 个实际加载 JS 路径的状态/哈希和 CSS 双向隔离。原始运行的整体失败状态与恢复性告警保留，不将功能断言通过写成全部日志为零。 |
| 最后两项 UI 增量 | 详情两阶段共用静态骨架，141 个相关用例及两尺寸几何检查通过；预览短内容收缩、长内容封顶、展开收起与重新加载通过 89 个相关用例及 12 项浏览器检查，最终产物另完成上行回归。 |

生产回归保留恢复性 hydration 观察：原始多轮日志共记录 11 条 React #418，涉及 `/`、`/login`、`/admin/submission`、`/admin/rating-report`、`/preview/submission/1`；其中首次带有临时主题夹具故障的轮次仍单独留存。有效复验在前台与旧举报页仍有告警，两次全新会话冷开登录页正常且输入可用；两路预览的深浅色、嵌入与独立打开及导航功能均通过。Astra 未发现本轮重大流程失效或新增代码回归证据，判定不阻断候选提交。原始 `passed: false`、功能断言通过和夹具修正分别记录，不声称全绿、已修复或与历史同编号告警为同一根因。

数据边界另记：基础 8 组回归的 36 张表摘要一致；补验的真实 `GET /api/user/session` 经既有 `getStatus` 更新了临时用户 1 的 `last_login_time`、`updated`，只读计算排除这两字段后用户表摘要一致，其余 35 张表未变。未实际还原数据。仅将该会话读取替换为明确标记的只读响应后，四方向导航复验的 36 张表摘要一致；管理接口与两路预览鉴权未替换。此记录不概括为整个补验零数据库写入，也不把会话元数据更新当作审核业务变更。

本轮汇总与原始日志位于本机 `/private/tmp/otoame-m01-readiness-20260911/acceptance.md`；骨架与预览增量证据分别位于 `/private/tmp/otoame-m01-loading-20260911/acceptance.md`、`/private/tmp/otoame-m01-preview-fit-20260911/acceptance.md`。旧后台未逐个执行的历史写动作仍保持前文覆盖边界。本轮冻结并提交候选，不推送或部署；目标环境备份、preflight、适用迁移与 postflight、产物及运行配置核对按总计划发布流程执行，上线后完成一轮真实审核；模块 02 按 01 上线后开始的既定顺序执行。

### 10.1 工作顺序与并行交接

Codex 负责拆解、集成与验收组织；Astra 负责审阅，Luna max 负责后端实现，mkimi 的 K3 负责前端组件、交互和界面验证。优先复用作者会话实质互审；明确的小修改完成一轮修改后复审与针对性验证即可，有实质缺漏则继续修正。只处理当前需求涉及的正确性、权限和主要流程，不为凑轮数增加机制。以下只拆分既有 M01 任务；编排完成不代表实现或实验已经完成。

| 顺序与对应任务             | Codex 交付                                                     | 前端 agent 交付                                                           | 交接条件                                                                                 |
| -------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 契约准备（M01-1）          | 旧路径与逐动作权限盘点；第 5 节的共享类型、查询/响应和错误样例 | 按投稿审核流程核对界面需要的数据与状态，返回缺项                          | 读取、动作和预览的约定逐项明确后分别开工，不等待全部接口写完                             |
| 地基与样板（M01-2）        | 两根布局、鉴权、样式边界、锁定组件及依赖、最小页面入口         | 侧栏、标题栏、投稿列表与详情样板；统一密度、间距和操作样式                | 可导入组件和页面入口就绪后写 UI；先交可运行样板，接口与预览到位后再完成真实审核          |
| 四来源读取（M01-3）        | 列表、计数、单项、投稿详情接口及读取/权限用例，按端点交付      | 样板接真实接口后补齐四来源、筛选、深链接、加载/空态/失败/截断和刷新       | 前后端按同一类型并行；真实接口联调与 E01-02 对应证据成立后验收读取                       |
| 动作与预览（M01-4、M01-5） | 资源条件写、审核日志与计数；两个前台只读预览及其鉴权           | 先接通一条完整投稿审核，再补资源动作、确认、键盘和两个预览的内嵌/独立打开 | 预览实现可与读取/处理并行；涉及的读取、动作、预览分别就绪即接入                          |
| 集成收口（M01-5）          | 前台入口接线、最终构建、文档/技能同步与实验总验收              | 桌面/移动端、深浅色、键盘、错误恢复和旧后台往返验证                       | 按第 9 节汇合证据，E01-01 用待发布构建全项检查；E01-02/03 已有有效证据仅在相关变更后复验 |
| 用户管理早迁（第 6.9 节）  | 既有接口/权限、页面入口与旧地址承接                            | 用户查询和既有动作、萌萌点明细的新界面及界面用例                          | 主流程基本完成且样式隔离/旧后台回归通过后启动，可与核心收尾并行；不阻塞 01 上线          |

契约准备把列表/计数的完整返回结构、各来源 payload、单项三态、动作请求体、冲突提示、日期序列化及页面组件 props 落成共享类型和少量测试样例。来源筛选、分组、选中项的 URL 编码统一；搜索的作用字段、范围与计数关系已由用户批准并落实于第 5.2 节；前端按同一契约接入。调用沿用第 5.1 节的请求工具和冲突常量。

后台样板以官方 shadcn 组件与布局块为基础，围绕投稿审核组织页面；从一张样板收敛视觉规则，再扩展其他来源。样例数据只用于现有测试或临时展示，正式页面不保留假数据兜底；真实联调是功能交付条件。组件按当前需求引入，不搬整套后台工程，不增加独立 mock 服务、通用页面生成器或新数据层。

### 10.2 文件归属与运行安排

| 所有者     | 写入范围                                                                                                                                                                                                                                                                                                                                                                            |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Codex      | `app/` 的路由移动、根/段布局、鉴权、页面薄入口和 API；`middleware*`、前台预览与既有前台组件/导航；共享 `types/`、`validations/`、`constants/` 和请求工具；`package.json`、lockfile、`components.json`、样式入口及扫描边界；`lib/dashboard/utils.ts`、生成的 `components/dashboard/ui/**` 及模板附带的基础 hooks（按实测生成清单逐文件指定）；接口/服务/集成测试及项目文档、技能同步 |
| 前端 agent | `components/dashboard/**` 中除基础 `ui/` 外的后台业务组件，`hooks/dashboard/**` 中按需新增的业务 hooks（排除 Codex 负责的基础文件），以及交接时逐文件指定的界面测试                                                                                                                                                                                                                 |

Codex 先创建最小挂载入口；前端交组件导出和约定 props，Codex 接线。缺组件、主题调整或共享类型变更由前端附路径、用途和最小差异回传，Codex 修改后通知作者；双方不同时改共享文件。生成组件、安装依赖、改 lockfile 和全局配置统一由 Codex 执行。

派发时记录代码基点与可写文件，前端读取范围排除所有 env 文件和凭据；不得自行提交、推送、部署或直接操作数据库。开发服务、隔离环境与生产构建由 Codex 按第 9 节和项目开发指南统一调度，同目录只运行一个开发服务，开发与生产构建不并跑。前端可运行约定的界面单元测试，在已交付的隔离服务上检查页面与约定的审核操作，不自行另起环境。

派发包明确前台与旧后台继续使用 HeroUI、新后台使用 shadcn 的已定边界。`otoame-frontend` 已按两套根布局划分组件与主题规则，`otoame-api` 已明确管理员审核数据的权限及缓存边界。

### 10.3 交付与实验分工

前端每包交实际改动文件、组件/接口对接情况、已执行测试与浏览器检查结果、未验证项。样板和最终页面均核对长中文内容、错误反馈、快速切项与轮询响应的先后关系；旧请求不能覆盖当前选中项，键盘不能干扰表单输入。沿用现有测试工具，不为委派新增测试平台。

| 实验   | Codex 负责的证据                                                                                        | 前端 agent 负责的证据                                                                                                |
| ------ | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| E01-01 | 路由/脚本/CSS 产物、双向样式边界、状态码与鉴权、同源嵌入限制、预览相关业务数据行前后对照                | 两预览的内嵌/独立打开和请求记录，深浅色、移动端、缩放、404 与跨布局往返；截图配合产物和数据证据                      |
| E01-02 | 按第 9 节造数；NULL 起点、最老项、筛选/偏好与单项状态的接口用例；权限与受控并发下的业务、日志、通知证据 | 最老项和截断可见、筛选/深链接状态、只读来源和角色入口、动作后的选中/刷新、冲突反馈、键盘与请求竞态                   |
| E01-03 | 专用日志与计数、注入时间跨日、再次审核和不同管理员的 API/数据证据、旧路径逐动作权限                     | 每 60 秒及动作后的计数刷新，旧页面按既有角色逐项核对可达性/操作与权限提示、旧标识和返回入口、顶栏/个人页及旧直链表现 |

双方按第 9 节的既定时间点交证据，Codex 核查并作最终验收；未执行项继续标待执行。前端请求保护不替代服务端并发证据，界面权限不替代接口鉴权。早迁页面另外完成第 6.9 节的页面用例，保留 role 3 的账本访问，不能用用户父布局统一拦截；发布和稳定使用后的旧页退役仍记入总计划已有迁移表。

## 11 非目标

- 不把其余后台页面迁移设为 01 核心交付的前提；第 6.9 节可逐项承接稳定页面，资源管理仍归 06。
- 不改处理服务的业务语义，不引入可撤销机制。
- 首批待审事项固定四来源，创作者申请使用独立管理页；旧反馈与旧举报只读，不接处理动作。
- 不做实时推送，只做 60 秒计数轮询与动作后刷新。
- 不重做资源编辑表单；不做队列层的举报分组统计；不建通用详情路由框架，深链接只按来源与 ID 单条取项。
- 不新增表、字段、索引、定时任务、消息总线、队列框架、规则后台、角色层级；不为将来模块预留字段或状态；不预建来源注册表。02 扩展既有举报目标，03 接入事项，08 在既有投稿来源内增加纠错类型；其余管理能力按所属模块迁移，本模块只保证接入位置明确。
- 不删除尚未迁移页面的组件、路由与面包屑映射；已完成早迁的页面按第 6.9 节逐页退下旧界面。不处理旧反馈与旧举报的归档迁移（模块 09）。

## 12 设计决定与实施验证

2026-09-10 已确认：用户管理早迁，其余按建议执行。下表为当前决定；验证结果和实现版本另行记录。

| 编号 | 事项           | 已定做法                                                                                                       | 后续工作                                     |
| ---- | -------------- | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| D1-a | 待审事项来源     | 首批四来源；创作者申请不入队，迁移时使用独立页                                                                 | M01-1 记录，M01-3、M01-4 落实                |
| D1-b | 旧反馈与旧举报 | 待审事项只读；管理员可读，处理仍限超级管理员并跳旧后台                                                           | M01-3、M01-4                                 |
| D2-1 | 控制台命名     | URL 前缀为 `/dashboard`；新后台路由组、组件、样式、工具目录及其配置引用统一使用 `dashboard`，具体路径见第 6 节 | M01-2                                        |
| D2-2 | 请求通道       | 新控制台走 HTTP API，复用现有接口与业务服务；不增加页面 server action 写入通道                                 | M01-3、M01-4；早迁账本复用既有 HTTP 查询接口 |
| D2-3 | 审核确认     | 所有审核动作（含快捷键）先确认后提交；旧后台已有确认沿用；不实现撤销                                                               | M01-4                                        |
| D2-4 | 用户管理       | 用户管理与用户萌萌点明细按第 6.9 节早迁；用户列表/账号管理仍为 `>= 4`，发放点数与管理员账本访问保持 `>= 3`     | 01 早迁任务；09 核对已迁移结果               |
| D2-5 | 预览布局       | 两种预览均在前台根布局下，同源 iframe 内嵌并可独立打开                                                         | M01-5 与 E01-01                              |
| D2-6 | 未匹配地址     | 先实测普通兜底；若确需实验性全局兜底，提供结果确认后启用                                                       | 普通兜底与缺失文档 404 已在实际控制台隔离构建验证，不启用实验特性          |
| D2-7 | 测试工具       | 使用现有工具，不引入新的组件测试工具                                                                           | 按第 9 节验证                                |
| D2-8 | 统计首页       | 本轮迁入 `/dashboard`，保留原统计并补已批准计数，完整统计保持 `role >= 4`                                                                                                   | 第 6.10 节；09 接后续运营指标与快照                       |
| D3   | 后台依赖       | 按锁定模板核对实际 imports、依赖与隔离产物                                                                     | 第 6.3 节已记录锁定版本；E01-01 两套产物边界已核对                 |

首批审核采用逐项操作与键盘导航，不做批量审核。设计决定已定，不再因这些选择等待确认；M01-1 路由与权限盘点、D2-6 与 D3 的隔离验证已有记录，完整交付状态见第 10 节。上述决定不批准其他模块的候选产品参数，不代表构建、测试、迁移或上线已经完成。
