# 模块 02：小喇叭、官方置顶与横幅（实施计划）

状态：基础实现与隔离验收完成；本轮首页展示与公开历史分页修订已通过相关测试、类型检查和浏览器检查，待站长验收。2026-09-12 已完成全量测试、生产构建、迁移演练、三个隔离实验、真实 Chrome 验收与本地维护重启演练；未执行生产迁移、部署或发布，实验过程未读取环境变量文件。

配对：[项目管理视图](./02-shoutbox-pm.md)。上级：[总计划](./00-master-plan.md)。基线：[运营层设计基线](../7.operations-layer-redesign.md)第 9.1 至 9.4 节。任务编号沿用总计划第 4.3 节的 M02-1 至 M02-5。

## 1 目标与基线

| 目标                                                                            | 基线     | 任务  |
| ------------------------------------------------------------------------------- | -------- | ----- |
| 付 50 点发布不可回复短消息，可选关联一个游戏；官方免费                          | 9.1、9.2 | M02-1 |
| 首页 15 条滚动列表；全站与游戏每页 20 条，可翻阅全部公开历史 | 9.1（站长于 2026-09-12 调整） | M02-2 |
| 游戏页最新 3 条关联消息与「更多」                                               | 9.2      | M02-2 |
| 官方生效区间、置顶占一个名额、重要级渲染全站可关闭横幅                          | 9.2      | M02-3 |
| 每条带举报入口、达阈值自动隐藏、关键词兜底、有处理与恢复端                      | 9.1      | M02-4 |
| 维护前发重要级官方消息、恢复后更新，清单进运维文档                              | 9.4      | M02-5 |
| 不群发、不做应用内维护模式、不关联资源、不做用途审查、不改 MDX 体系             | 9.1–9.5  | 全部  |

只读核查到的实现前提：首页是静态渲染加 120 秒聚合缓存（[app/api/home/service.ts](../../app/api/home/service.ts)、[config/cache.ts](../../config/cache.ts)），所以小喇叭与横幅不能进首页静态载荷；匿名公开接口的共享缓存头固定含 `stale-while-revalidate=300`（[app/api/utils/cacheHeaders.ts](../../app/api/utils/cacheHeaders.ts)），无法表达生效边界；`getOrSet` 支持 `shouldCacheValue` 与 `isCachedValueValid`，`delKvPattern`、`purgePublicApiCache` 已存在（[lib/redis.ts](../../lib/redis.ts)、[app/api/patch/cache.ts](../../app/api/patch/cache.ts)）；账务已有可用余额条件更新与账本幂等键（[app/api/moemoepoint/service.ts](../../app/api/moemoepoint/service.ts)、[prisma/schema/moemoepoint.prisma](../../prisma/schema/moemoepoint.prisma)）；`patch_report.patch_id` 为非空外键（[prisma/schema/patch-report.prisma](../../prisma/schema/patch-report.prisma)），旧举报服务的序列化、通知链接、缓存失效都直接用 `report.patch`，`getReport` 按单一 `targetType` 且 `created desc` 取数（[app/api/admin/report/service.ts](../../app/api/admin/report/service.ts)），旧举报只读路由要求 `role >= 4`（[app/api/admin/report/route.ts](../../app/api/admin/report/route.ts)）；既有举报创建是先查后插、无并发保证（[app/api/patch/comment/report/service.ts](../../app/api/patch/comment/report/service.ts)）；周期任务集中在 [server/cron.ts](../../server/cron.ts)。

## 2 偏离与理由

| 偏离                                           | 基线            | 做法与理由                                                                                                                                                                                            |
| ---------------------------------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 官方消息不受 5 分钟编辑限制                    | 9.1             | 用户消息严格一次；官方可随时改正文、级别、区间、链接。9.4 要求维护后更新公告，窗口远超 5 分钟，且编辑窗口是对付费内容的防滥用规则                                                                     |
| 首页与游戏页的小喇叭由客户端组件取数           | 未规定          | 首页 `force-static`、条目页 ISR，静态载荷会把生效与过期边界冻住，并迫使每次发布清理页面缓存                                                                                                           |
| 全部公开历史均可分页查阅 | 9.1 | 站长于 2026-09-12 确认首页 15 条、分页 20 条，全站与游戏都保留公开历史；可见性继续由状态与官方生效时间判断，不清理消息行 |

| 举报接入现有 `patch_report`，`patch_id` 改可空 | 3.2             | 总计划 M02-4 要求阶段 2 前接旧举报补目标类型；新建表会在模块 03 合并时多一次迁移                                                                                                                      |
| 不新增周期任务                                 | 总计划 4.3、5.3 | 生效与过期由查询时刻判定、缓存寿命夹到下一个时间边界，没有需要 cron 承担的正确性工作，也没有要清理的数据；见第 7 节                                                                                   |

## 3 术语

| 术语                | 定义                                                                                                                                                                                                                             |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 可见基础条件        | `status = 0 AND (official = false OR effective_from <= now)`，首页、小喇叭页、游戏页关联条与他人视角的作者页一律套用；未生效官方行只在控制台列表可见；过期官方行进入时间线并带官方标记                                           |
| 展示槽位 / 分页范围 | 首页固定最多 15 个槽位；全站与游戏分页每页 20 条，按全部公开记录计算页数。全站生效中的官方置顶占首页及分页第一页的一个名额，时间线查询排除该条 |
| 置顶 / 横幅         | 置顶取最新 `official AND status = 0 AND effective_from <= now < effective_to`（任意级别）；横幅取其中最新的 `level = 'important'`，同样显式要求 `status = 0`，撤回的官方消息两处都不出现。两者可能不是同一条，过期时点也可能不同 |
| 时间线查询          | 按 `created desc, id desc` 且排除当前置顶那条的公开查询，避免同页重复                                                                                                                                                            |
| 时间边界            | 置顶与横幅的 `effective_to`、下一条待生效官方的 `effective_from` 中最早者                                                                                                                                                        |
| 误判恢复            | 站方把隐藏或删除的消息改回公开，首次恢复触发唯一一次退款                                                                                                                                                                         |
| 显示掩码            | 保留消息、只清空其关联游戏名称与链接，用于对仅限全龄的请求隐藏 NSFW 条目信息                                                                                                                                                     |
| 状态 0/1/2/3        | 公开 / 作者自删 / 隐藏待复核 / 站方终止。状态 3 在用户消息上是「违规删除」，在官方消息上是「已撤回」，界面与通知按 `official` 取文案，不给官方消息挂违规标签                                                                     |

## 4 数据模型

### 4.1 `prisma/schema/shoutbox.prisma`

模型 `shoutbox`，命名沿用既有 schema 风格。

| 列                                | 类型                                                                    | 说明                                                                         |
| --------------------------------- | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `id`                              | `Int @id @default(autoincrement())`                                     | 主键，横幅关闭状态与举报都按它引用                                           |
| `user_id`                         | `Int` + 关系 `onDelete: Cascade`                                        | 作者；官方消息是执行发布的站方账号                                           |
| `request_id`                      | `String @db.VarChar(64)`，`@@unique([user_id, request_id])`             | 客户端 UUID，按用户维度唯一。不设全局唯一，避免用他人 UUID 读到他人内容      |
| `content`                         | `String @db.VarChar(500)`                                               | 校验层限 200 字，列宽留余量                                                  |
| `link`                            | `String @default("") @db.VarChar(1000)`                                 | 官方详情链接（9.3 的长文），非官方必须为空串                                 |
| `official` / `level`              | `Boolean @default(false)` / `String @default("normal") @db.VarChar(16)` | `normal` / `important`，仅对官方有意义                                       |
| `status`                          | `Int @default(0)`                                                       | 见术语表                                                                     |
| `cost`                            | `Int @default(0)`                                                       | 本次实际扣费；退款按它，调价不影响历史                                       |
| `patch_id`                        | `Int?` + 关系 `onDelete: SetNull`                                       | 只能是条目；条目删除时消息保留、关联清空                                     |
| `effective_from` / `effective_to` | `DateTime?`                                                             | 官方生效区间，非官方必须为空                                                 |
| `edited_at`                       | `DateTime?`                                                             | 非空即用掉唯一一次编辑机会                                                   |
| `hidden_at`                       | `DateTime?`                                                             | 进入 status 2 时写入，恢复时清空；用于「隐藏中」筛选与展示，不依赖 `updated` |
| `refunded_at`                     | `DateTime?`                                                             | 非空即误判退款已发生                                                         |
| `created` / `updated`             | `DateTime`                                                              | 常规时间列                                                                   |

索引（按查询需要）：`[status, created desc, id desc]` 时间线与计数；`[patch_id, status, created desc]` 游戏页；`[user_id, created desc, id desc]` 作者页；`[official, status, effective_to]` 置顶与横幅。

约束（写在生产 sync SQL，Prisma 无法表达；本地 `prisma db push` 不会创建，因此约束类断言必须在由 sync SQL 建库的隔离库上跑）：`status IN (0,1,2,3)`；`level IN ('normal','important')`；`cost >= 0`；形态约束——官方行两个时间非空且 `effective_to > effective_from`，非官方行 `level = 'normal'`、两个时间为空、`link = ''`。最后一条是安全不变量：任何路径都不能让用户消息变成全站横幅。

### 4.2 修改 [prisma/schema/patch-report.prisma](../../prisma/schema/patch-report.prisma)

`patch_id` 由 `Int` 改 `Int?`（未关联游戏的小喇叭没有所属条目，这是本模块唯一的既有列语义变更）；新增 `shoutbox_id Int?` 与关系 `onDelete: SetNull`；新增 `@@index([target_type, shoutbox_id, status])`；`target_type` 增加取值 `'shoutbox'`（`VarChar(16)` 够用）。不改 `status` 语义，不加权重或计数列。

### 4.3 不新增

横幅关闭状态表（按基线存浏览器本地）、关键词表、举报权重表、官方模板表、消息与游戏的多对多、处置人列（用 `admin_log`）。

## 5 接口、校验、权限、幂等

沿用仓库约定：route 解析与鉴权、service 归属与事务，解析用 `kunParse*`，失败以 HTTP 200 返回中文字符串，写请求经既有 CSRF 与来源校验。校验在 `validations/shoutbox.ts`、常量与文案在 `constants/shoutbox.ts`、类型在 `types/api/shoutbox.ts`。不改 `middleware.ts`，不为本模块加鉴权排除项。

普通与官方消息的发布、编辑统一先将正文中的手动换行替换为空格，再做首尾去空白、长度与关键词校验；输入框与历史消息展示使用同一规则，按可用宽度自然换行。该规则只作用于小喇叭正文，不改变举报原因和审核回复。

### 5.1 公开读接口（`app/api/shoutbox/route.ts`、`service.ts`）

`GET /api/shoutbox`：首页使用 `view=home`，固定读取第 1 页的 15 个槽位；常规全站及游戏分页每页 20 条，`page` 校验正安全整数，`patch` 可选且须匹配八位 `unique_id`。返回 `{ pinned, shoutboxes, page, totalPages, validUntil, visibilityUntil }`（首页另带 `hasMore`），`pinned` 只在全站第 1 页或首页可能非空。首页模式不和 `patch` 或其他页码混用。

- 全站分页：有置顶时第 1 页取时间线 `skip 0 take 19`；第 N（>1）页 `skip (N-1)*20-1 take 20`。无置顶时 `skip (N-1)*20 take 20`。`totalPages = ceil(公开条数 / 20)`，不截断公开历史。首页同理取 14 条加置顶或 15 条普通时间线消息。
- 游戏分页：`可见基础条件 AND patch_id = X`，按 `created desc, id desc` 排序，每页 20 条；游戏关联条显示该集合最新 3 条。所有公开消息均可查阅，不按年龄过滤，也不依赖全站置顶与曝光范围。
- 两种分页均按实际集合计算页数；`page > totalPages` 返回空列表与真实页数，不读取越界页数据。查询保留已有稳定排序和可见性约束，不新增数据表或分页框架。
- 掩码：载荷带条目分级，序列化时按请求偏好清空 NSFW 条目的关联信息，消息本身保留。掩码在缓存之后、响应之前执行，Redis 只存一份全集载荷。

`GET /api/shoutbox/banner`（只导出 GET）：无参数，返回最新生效重要级官方消息、`validUntil` 与 `visibilityUntil`，没有则 `null`。

作者个人页沿用 `/user/:path*` 的登录边界，数据走服务端读取、不进 Redis、响应 `private, no-store`；本人与站方看全部状态并显示状态标签，其他已登录用户按可见基础条件过滤——不只是 `status = 0`，站方账号名下未生效的官方行同样不可见。

### 5.2 用户写接口

| 方法     | 校验与权限                                                                                           | 事务与幂等                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `POST`   | 登录；`requestId` 为 UUID；正文去空白后 1–200 字；`patchId` 可选且须存在；关键词命中在进入事务前拒绝 | 单事务内先 `INSERT` 再 `spendMoemoepoint(50, requiredAvailable 50, idempotencyKey = shoutbox:publish:{userId}:{requestId})`。唯一冲突（P2002/23505）时整笔回滚，然后在事务外按 `(user_id, request_id)` 重读：只有确实读到本人这条记录、且它的 `official` 与本次请求类型一致时才作为幂等成功返回，读不到就按真实错误上报，不用「任何唯一冲突都算重放」掩盖别的约束冲突；不能在已中止的事务里继续查询；`40001`/P2034 做最多 2 次有限重试 |
| `PUT`    | 登录；本人；正文长度与关键词校验与 `POST` 共用同一 schema 和拒绝分支                                 | 条件更新 `id AND user_id AND official = false AND status = 0 AND edited_at IS NULL AND created > now - 5 分钟`；命中 0 行再读一次给出具体原因                                                                                                                                                                                                                                                                                          |
| `DELETE` | 登录；本人                                                                                           | 条件更新 `id AND user_id AND official = false AND status IN (0,2) → 1`。允许自删已被自动隐藏的消息，不静默禁止；自删不退款、不改举报状态，站方对它只能用 `resolve` 结案，不恢复也不退款                                                                                                                                                                                                                                                |

`cost` 与 `official` 只由服务端常量决定，不接受客户端传值。同一用户复用已用过的 `requestId` 但正文不同时以首次写入为准：返回既有行，不更新正文、不二次扣费，也不做载荷指纹比对；若既有行的 `official` 与本次请求类型不符，返回冲突提示且不改行，避免一次付费请求被当成官方发布。不做注册时长门槛、频率限制与同文去重。

### 5.3 举报（`app/api/shoutbox/report/route.ts`）

登录；不能举报自己的消息；发布者当前为超级管理员（`role = 4`）时，普通与官方消息均拒绝新举报，已有举报保留可结案。其余官方消息保留举报入口，正常落库进复核队列，但不参与自动隐藏。读取 DTO 提供服务端计算的 `reportable` 布尔值，作者公开字段仍只有 id、name、avatar；前台隐藏不可举报消息的举报入口，管理员的后台审查导航保留。事务顺序：`SELECT ... FOR UPDATE` 取目标行 → 读取作者当前角色并拒绝超级管理员消息 → 查同一举报人是否已有该目标的待处理举报 → 插入举报行（`target_type = 'shoutbox'`、`shoutbox_id`、`patch_id` 取消息关联或 `NULL`、`reported_user_id` 取作者）→ 统计该消息待处理举报的不同举报人数 → 达到 3 人且 `official = false` 时条件更新 `status = 0 → 2` 并写 `hidden_at`，只有这次更新命中 1 行才给作者发一条通知。行级锁让先查后插的重复检查与阈值计数都具备并发正确性，事务极短，不用 Serializable 加重试。取到目标行后先核对可举报资格：只有 `status = 0` 的消息接受新举报；未生效的官方消息（`official AND effective_from > now`）、已撤回或已删除（`status = 3`）、作者已自删（`status = 1`）的消息一律拒绝，避免靠猜 ID 给不可见内容制造举报；已进入隐藏待复核（`status = 2`）的用户消息允许继续接收在途举报，它们作为同一待办的补充证据，不再触发第二次隐藏。目标在举报之后才被自删的，旧举报仍可用 `resolve` 结案。通知用 `createMessage`，不用先读后写的 `createDedupMessage`。成功新增举报的事务提交后，以 best-effort 向 `role >= 3` 管理员发系统通知，链接 `/dashboard/shoutbox?shoutbox=<id>`；查收件人或发送失败不得撤销举报，某个收件人失败不阻断其他收件人。重复或拒绝的举报不发送该通知。

### 5.4 站方接口与旧举报读取面

| 接口                                | 门槛        | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /api/admin/shoutbox`           | `role >= 3` | 可选 `shoutboxId` 精确读取单条（跨状态、含待处理举报）；未带 ID 时列表与筛选 `tab = pending_review \| public \| removed \| official`；`pending_review` 覆盖两类并集：存在待处理举报的消息（含官方与作者已自删的）与所有 `status = 2` 的消息（含站方主动隐藏、举报已结案的），按「最早一条待处理举报的时间，没有待处理举报时按 `hidden_at`」正序，保证最老待办可达且隐藏中的消息不会无处可查；其余按 `created desc`；响应 `private, no-store`                                                                                                                                                                                                |
| `POST /api/admin/shoutbox`          | `role >= 3` | 发布官方消息：正文、级别、`effectiveFrom`（默认当前）、`effectiveTo`（默认 +72 小时）、可选链接、`requestId`；`official = true`、`cost = 0`，不写账务                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `PUT /api/admin/shoutbox`           | `role >= 3` | 改官方正文、级别、区间、链接；`action = end` 把 `effective_to` 提前到当前，条件是 `official = true AND status = 0 AND effective_from < now AND effective_to > now`（严格小于：`effective_from = now` 会写出 `effective_to = effective_from` 而违反约束），对未生效、恰好开始与已过期的消息不执行并返回提示，重复 `end` 命中 0 行、无副作用，也不会把已过期的区间延长回当前；`action = cancel` 是软撤回，条件更新 `official = true AND status = 0 → 3`，未生效（含恰好开始）与生效中的官方消息都用它，撤回后不进任何列表也不会到点生效。条件更新一律要求 `official = true` |
| `POST /api/admin/shoutbox/moderate` | `role >= 3` | `hide`：`0 → 2` 并写 `hidden_at`；`remove`：`IN (0,2) → 3`；`restore`：`IN (2,3) → 0` 并清 `hidden_at`；这三个动作带 `official = false` 守卫。`resolve`：带结论（受理或驳回）与回复文案，只给该消息的待处理举报下结论并通知，不改消息状态，守卫 `status <> 2`（隐藏中的消息只能 `remove` 或 `restore`）——它是官方消息与作者已自删（`status = 1`）消息的举报的唯一闭环；官方内容本身的处置走 `PUT` 的 `end` 或 `cancel`                                                                                                                                                    |

退款与状态恢复分开：`restore` 的 CAS 不含 `refunded_at` 条件，第二次误判也能恢复可见；退款只在同事务内满足 `cost > 0 AND refunded_at IS NULL` 时执行一次（账务幂等键 `shoutbox:{id}:restore-refund`），因此累计退款不超过实付——这是账务硬约束，不列入待定项。动作与举报的关系按下表，`hide` 只隐藏、不结案，否则隐藏待复核的消息会从待办队列里消失：

| 动作      | 消息状态                 | 该消息的待处理举报           | 通知                                   | 账务           |
| --------- | ------------------------ | ---------------------------- | -------------------------------------- | -------------- |
| `hide`    | `0 → 2`                  | 保持待处理                   | 只通知作者                             | 无             |
| `remove`  | `IN (0,2) → 3`           | 全部置为已处理               | 作者与每个举报人                       | 无             |
| `restore` | `IN (2,3) → 0`           | 全部置为已驳回               | 作者与每个举报人                       | 首次恢复退一次 |
| `resolve` | 不变，守卫 `status <> 2` | 全部按结论置为已处理或已驳回 | 每个举报人，结论涉及作者时一并通知作者 | 无             |

`resolve` 用于不需要改消息状态的结论：官方消息、作者已自删的消息、票数不足未触发隐藏的普通举报；它不能对 `status = 2` 使用，隐藏中的消息只能 `remove` 或 `restore`，否则会留下一条隐藏却没有待办的消息。没有待处理举报时 `resolve` 是幂等空操作：不发通知、不写审计。官方 `PUT` 的重复请求靠条件更新命中 0 行来保证无副作用，不额外声称它由状态机保证幂等。作者已自删的消息只能 `resolve`，结案并通知，不恢复、不退款（基线「自删不退」）。

处置与结案在同事务内先对目标行 `FOR UPDATE`，再按 `shoutbox_id` 选取并更新当时的待处理举报、发上表要求的通知、写一条 `admin_log`（`shoutbox_official_publish` / `shoutbox_official_update` / `shoutbox_moderate`，后者覆盖 `hide`、`remove`、`restore`、`resolve`）；处置期间并发提交的新举报要么被这次结论覆盖，要么留在待处理里等下一次，不会漏通知也不会重复通知。

给模块 03 的接线合同只有一条：消息状态转换与退款这两个领域操作接受调用方传入的事务句柄（与账务原语同一形态），03 接手后在自己的工单事务里按结论调用它们并自行发通知，不需要回头调用按 `reportId` 入参的旧处理器。本期不预造无人调用的适配函数，切换时再最小抽出。门槛依据基线 2.2：后台队列统一管理员级，本模块不物理删除数据，不属破坏性清单。

旧举报侧只做最小改动：`adminReportTargetTypeSchema` 增加 `'shoutbox'`；`serializeReport` 的 `targetType` 与 `patch` 改为可空安全并补 `shoutbox` 子对象；`buildReportNotice` 的类型标签与通知链接在 `patch` 为空时改用小喇叭链接；`handleReport` 对 `target_type = 'shoutbox'` 直接返回「请在控制台小喇叭复核页处理」，不在旧路径重复实现处置，也不动旧路由 `role >= 4` 的门槛；`recomputePatchRatingStat` 与条目缓存失效只在 `patch` 非空且目标为评论/评价时调用。可空适配要覆盖全部引用点，已确认的有 [components/admin/report/ReportCard.tsx](../../components/admin/report/ReportCard.tsx)（第 157 行用 `report.patch.name`）与 [components/admin/report/ReportHandler.tsx](../../components/admin/report/ReportHandler.tsx)（第 19 行用 `report.patch.uniqueId`），实现时按 `report.patch` 与 `AdminReport['patch']` 的引用清单逐处过一遍，不只改类型。模块 01 的统一收件箱把举报作为四来源之一、在同一次查询里覆盖多种目标类型，本模块只在它的谓词与详情映射里增加 `shoutbox`，不新增第五个来源，也不改 01 已定的门槛；旧 `getReport` 仍按单一 `targetType` 取数，除可空适配外不动。评论与评价的举报创建端不变。

### 5.5 缓存、响应头与失效

缓存由 `app/api/shoutbox/cache.ts` 实现；首页、全站分页、游戏分页与横幅使用独立键。各身份共用原始数据缓存，返回前按请求的分级及屏蔽偏好处理关联游戏；个性化 HTTP 响应不影响内部缓存复用。读取键附带独立修订号，横幅原始载荷使用 v4 键隔离旧序列化结果。基础时长写进 [config/cache.ts](../../config/cache.ts)（列表与横幅 60 秒、游戏页关联条 300 秒）。完整优化与验收要求见[缓存与加载优化计划](./02-shoutbox-cache-optimization-plan.md)。三步缺一不可：

1. 载荷自带 `validUntil`：从严格晚于当前、会改变本页可见集合的官方时间边界中取最早者，再与 `now + 基础时长` 取小。置顶与横幅关注当前消息的 `effective_to` 和下一条官方消息的 `effective_from`；游戏分页关注关联官方消息的开始时间。消息年龄不参与可见性与缓存到期计算。
2. Redis 寿命 `ttl = floor((validUntil - now) / 1000)` 秒，小于等于 0 就不写缓存；读取时用当前时刻检查载荷有效期，越界即经 `getOrSet` 合并重建。每次实际构建重新取当前时间；构建结束跨界时最多重建两次。数据库或偏好读取失败、有限重建耗尽时返回 `503` 和通用 JSON 错误字符串，响应为 `private, no-store`，`Retry-After` 为 60 秒（游戏作用域 300 秒）；成功空列表与 `banner: null` 继续返回 `200`。
3. 响应头逐请求算：带登录或分级/屏蔽偏好 cookie 的请求（判定复用 `isPersonalizedApiRequest`）返回 `private, no-store`；匿名请求返回 `public, s-maxage=floor(min(30, (validUntil - now) / 1000))`，单位秒、向下取整、没有下限，且不带 `stale-while-revalidate`，算得 0 秒时返回 `no-store`。这就是不复用共享匿名缓存层的原因：那层固定含 5 分钟陈旧供给，会跨过生效边界。

写入后失效：用户发布/编辑/自删、站方处置、官方发布/编辑/提前结束，在事务提交后先等待独立键 `shoutbox_revision:v1` 的递增尝试，再执行 `delKvPattern('shoutbox:*')` 与 `purgePublicApiCache(['/api/shoutbox', '/api/shoutbox/banner'])`。修订号使写前在途构建只能填充旧代次，写后新读使用新代次；已开始的读可以返回当时快照。递增或清理失败分别记录，继续其余清理，不回滚业务、不将已提交的操作报为失败。读取修订号失败时跳过持久缓存，仅在本进程内按查询键合并在途构建，完成后释放。公共 Redis 和条目缓存基础设施不变。

### 5.6 幂等与并发一览

| 业务        | 数据库层保证                                                              | 重试或并发后的效果                                                                               |
| ----------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 发布与扣费  | `(user_id, request_id)` 唯一 + 账本幂等键 + 可用余额条件更新              | 一次发布一行一账；余额刚好 50 的并发只成功一次；不可能为负；他人重用同一 UUID 各自成功且互不可见 |
| 编辑 / 自删 | 条件更新含状态、`edited_at`、时间窗与 `official = false`                  | 第二次命中 0 行，内容不变，无账务                                                                |
| 自动隐藏    | 目标行 `FOR UPDATE` + `status = 0 AND official = false → 2`               | 并发举报只有一次转换成功，作者只收到一条通知                                                     |
| 站方处置    | 条件更新限定原状态集合 + `official = false`                               | 双请求只有一个生效，`admin_log` 与通知只记胜出的一次                                             |
| 误判退款    | 恢复 CAS 与 `cost > 0 AND refunded_at IS NULL` 的退款分支同事务但条件分开 | 状态可多次恢复，退款只一次，累计不超过实付；官方消息 `cost = 0` 不产生账务                       |
| 重复举报    | 行锁内的待处理举报检查                                                    | 第二次返回已举报文案，不插入新行                                                                 |

事务内不做网络副作用；缓存与边缘清理在提交后执行。

## 6 前后台界面及文件

前台（HeroUI v2）：`app/(site)/shoutbox/`（页面、metadata，`revalidate = 0`，服务端取第一页交客户端容器翻页）、`app/(site)/user/[id]/shoutbox/`、`components/shoutbox/` 下的列表、单条卡片（官方消息以底色区分，显示生效的置顶与重要标签）、发布表单（含余额提示与固定的不可回复提示行；求助区在模块 07 上线前只作为纯文本提及，不给可点击链接）、举报入口、首页模块（客户端取数，HeroUI ScrollShadow 单列滚动 15 条，含头像、发布者、时间、发布按钮与「显示更多」；头像独立成列，右侧依次为置顶、重要标签和加粗的用户名，用户名后加中文冒号；正文可换行到标签下方，始终避开头像列。`sm` 及以上正文位于独立中列，不延伸至操作和时间下方；`sm` 以下首行只放头像、标签、发布者和操作时间，正文固定从第二行开始，跨文字与操作列；窄屏压缩标签内边距，过长用户名省略并以 title 提供全名；关联游戏为带手柄图标的内联胶囊，宽度不超过消息列与 12rem，同行放不下时整体换行；正文字号手机为 14px、`sm` 及以上为 16px，统一使用 24px 行高并自然换行；操作区按页面固定预留一个或两个 32px 槽位，按钮隐藏时不收缩；右侧为 64px 左对齐时间列，两者间距 2px。头像为 28px，与首行发布者、操作和时间居中对齐；自己的普通消息在可编辑的 5 分钟内显示编辑图标而不显示举报入口，管理员对他人消息的入口直达该条后台记录，管理员公告说明与入口仅在发布界面显示）、游戏页关联条（3 条加「更多」）、横幅组件。首页标题区只保留标题、同排放得下的费用标签和发布按钮；ScrollShadow 使用 `scrollbar-gutter: stable both-edges`，按实际滚动条宽度对称留白。首页 Card 使用与游戏卡片相同的 HeroUI `bg-content1`，随浅色与深色主题切换；整个 Card 上限为 300px，与上方 Hero 的最小高度对应，列表占剩余空间；首次加载使用同一总高度约束和单一 HeroUI v2 默认 Spinner，完整列表沿用相同加载样式，短内容自然收缩。首页查询多取一条判定 `hasMore` 后裁回 15 条，仅 `hasMore = true` 时在滚动列表末尾渲染导航至 `/shoutbox` 的「显示更多」，须滚到最后才可见。修改：[components/home/Container.tsx](../../components/home/Container.tsx) 插入首页模块、[components/patch/header/Container.tsx](../../components/patch/header/Container.tsx) 插入关联条（`isNsfwBlocked` 时不渲染）、[app/(site)/layout.tsx](<../../app/(site)/layout.tsx>) 挂前台横幅。

后台（shadcn，模块 01 的 dashboard）：`dashboard/shoutbox` 页面提供官方发布表单、四个页签列表与单条处置动作；`components/dashboard/shoutbox/` 用 01 锁定的组件集，不引入新依赖、不复用前台组件；dashboard 根布局挂一个同样按消息 ID 读写浏览器本地关闭状态、复用同一读接口的轻薄横幅组件。横幅覆盖前后台两套独立布局，样式各自实现，共享的只有取数与关闭键；投稿预览沿用 dashboard 预览壳的隐藏范围，不渲染横幅。

本模块新增：`prisma/schema/shoutbox.prisma`、`validations/shoutbox.ts`、`types/api/shoutbox.ts`、`constants/shoutbox.ts`、`utils/shoutboxTime.ts`、`app/api/shoutbox/**`、`app/api/admin/shoutbox/**`、三个 `migration/production-shoutbox-*.sql`。修改：[prisma/schema/patch-report.prisma](../../prisma/schema/patch-report.prisma)、[app/api/admin/report/service.ts](../../app/api/admin/report/service.ts)、`validations/admin.ts`、`types/api/admin.ts`、后台举报列表与详情组件、[config/cache.ts](../../config/cache.ts)、`constants/moemoepoint.ts`（增加 `shoutbox.publish` 与 `shoutbox.restore_refund` 两个原因码、消费规则「发布小喇叭 -50」、获得规则「小喇叭误判恢复 +50」，并写明自删与违规删除不退）、模块 01 的收件箱来源。不动：[app/api/home/service.ts](../../app/api/home/service.ts) 与首页渲染模式、[app/api/patch/cache.ts](../../app/api/patch/cache.ts)、共享匿名缓存层、[app/api/moemoepoint/service.ts](../../app/api/moemoepoint/service.ts)、`middleware.ts`、[server/cron.ts](../../server/cron.ts)。

文档同步（与代码同批交付）：运维文档的维护清单从 Telegram 版切到官方重要消息版，模块地图与测试文档补本模块。

## 7 定时任务

本模块不新增周期任务，也不改既有四个任务（[server/cron.ts](../../server/cron.ts)）。理由：生效与过期由查询时刻判定，Redis 寿命与 `s-maxage` 都向下取整夹到下一个时间边界，边缘不会跨界供旧值，因此没有需要 cron 承担的正确性工作，也没有需要清理的数据（本模块无删除路径）。加一个只做缓存失效的任务是空壳：`*/5` 的调度漏跑一轮就是 10 分钟空档，任何固定回看窗口都补不齐，而它本来也不承担正确性。

已打开的页面到 `validUntil` 时在后台重新取数，保留仍有效的画面并在成功后一次性替换。响应另带 `visibilityUntil`，记录下一次真实官方生效/过期边界；到此边界才执行可见性清理，慢请求或失败退避期间也必须按时生效。`null` 表示没有已知边界，旧响应缺少该字段时保守回退到 `validUntil`。重取失败仍按基础缓存时长退避，不热循环；普通公开历史不因年龄被移除。页面恢复可见时按当前时刻复核边界，再按需更新。不引入推送通道或新增可调参数。

总计划第 4.3 节 M02-5 的「周期任务沿用 `server/tasks/`」与第 5.3 节「官方消息生效/过期、可见范围维护」的任务责任行，按本节口径同步为「由查询时刻判定加缓存寿命夹取保证，本模块不交付周期任务」。

## 8 迁移与回填

三段式生产 SQL 已按定稿日实现：

1. `production-shoutbox-preflight-<日期>.sql`：`BEGIN TRANSACTION READ ONLY`。清点 `patch_report` 行数、`patch_id` 空值数（首次应为 0）、`target_type` 取值分布、是否已存在 `shoutbox` 表与同名约束、`patch_report` 现有索引与外键。只读无 DDL。
2. `production-shoutbox-sync-<日期>.sql`：`CREATE TABLE IF NOT EXISTS public.shoutbox`（含四类 CHECK、四条索引、作者 `ON DELETE CASCADE`、条目 `ON DELETE SET NULL`）；`ALTER TABLE public.patch_report ADD COLUMN IF NOT EXISTS shoutbox_id INTEGER` 并补外键与索引；`ALTER TABLE public.patch_report ALTER COLUMN patch_id DROP NOT NULL`。全部可重复执行，无数据回填——小喇叭是新能力，没有存量。
3. `production-shoutbox-postflight-<日期>.sql`：只读结构校验（列可空性、四类 CHECK、索引、外键存在）。行数与 preflight 快照的比对只在首次部署做；脚本重跑时不断言 `shoutbox` 为空表或举报行数不变，避免把结构校验和一次性基线混为一谈。

顺序与边界：备份 → preflight → 在审定窗口执行 sync → postflight → `pnpm prisma:deploy-safe`（前面含可写兼容迁移，不是纯只读检查）→ 构建部署 → 冒烟 → 最后开放用户发布与官方发布入口。生产不用 `prisma db push`，不放宽 schema 守卫。`DROP NOT NULL` 是目录级变更、锁时间极短，仍安排在窗口内执行。该序列已在隔离库完成，生产尚未执行。

回退：一旦有真实付费消息写入，不能盲目退到不认识 `shoutbox` 的旧代码（用户已扣的点会失去对应内容），预案是先关发布与官方发布入口（前台隐藏加接口返回停用文案），保留读取、处置与退款能力，再定位问题前滚。`patch_id` 恢复 NOT NULL 的前提是不存在空值行，已有小喇叭举报后必须先处理这些行，回退脚本先查后改，不预置反向 DDL。已发生的扣费与退款以账本为准，不做应用层反向补偿。

## 9 测试与实验方案

单元测试按实验编号归口，覆盖各自的纯函数与分支：归 E02-01 的有发布与编辑共用的正文及关键词校验、幂等键与账务入参、唯一冲突重读不到本人记录时按真实错误上报而不是伪装幂等成功、编辑与自删的条件更新形态、退款分支与金额取值；归 E02-02 的有首页 15 条与全站/游戏 20 条分页偏移、全部公开历史、置顶只占一条与跨页无重复、正安全整数页码及越界页、公开可见性与 NSFW 掩码、`validUntil` 官方生效过期边界、TTL 与 `s-maxage` 向下取整和 0 秒转 `no-store`、热缓存过期重取、横幅关闭记忆，以及首页发布后刷新与管理员官方发布入口；归 E02-03 的有权限矩阵、旧举报序列化与后台组件在 `patch` 为空时的分支、`handleReport` 对小喇叭的引导返回、四个处置动作与举报状态的对应矩阵（含 `hide` 不结案、`resolve` 拒绝 `status = 2`）、迁移 SQL 的静态锁定（preflight 只读、sync 幂等、postflight 只做结构校验）。`patch_report` 改动属共享基础设施，按仓库规则跑全量测试并加一次生产构建（根布局与首页组件有改动）。mock 不能证明原子性、真实并发、外键与 CHECK 行为，那些由下面三个实验负责。

三个实验编号固定，已于 2026-09-12 在由本模块 sync SQL 建立的隔离 PostgreSQL、独立 Redis 与独立本地生产构建中全部通过；未连接生产或真实 CDN，也未读取环境变量文件。E02-01 验证账务、幂等与并发，E02-02 验证基础版本的分页与保留规则、置顶与横幅时间边界，E02-03 验证举报、处置、权限、通知与缓存。全量测试为 256 个文件、2221 项通过，类型检查与生产构建通过。真实 Chrome 另验证前后台 1440/390、确认取消零写入、横幅生命周期和受控失败退避；维护演练完成同一公告 ID 跨实例重启与恢复后更新。Playwright 无法产生真实 `visibilityState = 'hidden'`，该恢复可见分支由组件测试覆盖，仍列入站长真实浏览器用户验收。

### E02-01 消费、编辑、删除与误判退款

- 目的：证明 50 点消费与三种退款边界在真实事务下守恒，重试与并发都不产生第二笔账，恢复能力不被退款资格阻断。
- 前提：三个账号余额 50 / 49 / 500，两个仅用于测试的关键词。
- 步骤：①余额 50 的账号用同一 `requestId` 并发发两次，再用新 `requestId` 发第二条；②对一次成功发布重放三次相同请求，再用同一 `requestId` 提交一次不同正文；③另一账号复用他人已用过的 `requestId` 发布；④余额 49 发布一次、命中关键词发布一次、把一条已发布消息编辑成命中关键词的正文一次；⑤编辑在第 4 分 59 秒与第 5 分 01 秒各一次，成功后再编辑一次；⑥删除路径：自删、从隐藏态自删、站方 `remove`、`hide` 后 `restore`、再 `hide` 再 `restore`；⑦对官方消息提交 `hide` / `remove` / `restore`；⑧作者编辑与站方 `hide` 并发提交；⑨作者自删一条已被举报的消息后，站方对它执行 `resolve`。
- 通过：并发只成功一条且余额 50→0；重放后一行一账、余额不变；同一 `requestId` 的不同正文不更新原行也不二次扣费；他人复用同一 UUID 各自成功且读不到对方内容；余额 49 与关键词命中都不产生消息行与账目，编辑命中关键词被拒且正文不变；只有第 4 分 59 秒那次编辑成功；自删与 `remove` 余额不变；首次 `restore` 余额 +50 且 `refunded_at` 落库，第二次 `restore` 恢复为公开但余额不变，累计退款等于 `cost`；官方消息的 `hide`/`remove`/`restore` 都被拒绝（结束官方消息只能用 `end` 或 `cancel`）；并发编辑与隐藏的结果等价于某个合法串行顺序——编辑先提交则两者都成功且最终为隐藏，隐藏先提交则编辑被拒且正文不变，不出现更新丢失或状态自相矛盾；已自删消息执行 `resolve` 后余额不变、不产生任何账目；全程 `moemoepoint - moemoepoint_reserved` 不为负。
- 失败：任一次重复扣费、重复退款、负余额、关键词命中仍扣费、退款金额与 `cost` 不一致，或第二次恢复无法把消息改回公开。
- 时间点：M02-1 与 M02-4 退款分支编码完成、sync SQL 定稿后，本批上线前。
- 证据与清理：账本导出（用户、方向、金额、原因码、幂等键、时间）、消息行状态快照、并发脚本与输出、失败断言记录。结束后 drop 隔离库、清该实例 Redis 前缀键，不向生产写入任何行。

### E02-02 分页保留、关联游戏、官方置顶与横幅过期

- 目的：首页最多 15 条手动滚动；全站与游戏每页 20 条并可翻阅全部公开历史；官方置顶与横幅独立生效、过期与更新。
- 查询验证：用足够跨页的公开消息覆盖新旧时间、官方/普通、不同游戏和分级，以及隐藏、删除、撤回、未来生效状态。验证有无置顶下的首页、第 1/2 页和末页无重复遗漏；全站与游戏均可查到超过三个月的公开消息；越界页返回空数据及真实页数；掩码只隐藏关联信息。
- 时间与缓存验证：在官方开始/结束边界前后读取，确认首页、分页、游戏与横幅更新；保留提前结束、撤回、关闭记忆、可见性恢复与失败退避的既有验证。缓存键区分首页与分页，读取不复用旧契约载荷，`validUntil` 和响应头不跨过官方时间边界。
- 浏览器验收：首页头像、发布者、正文、时间在桌面和手机可读；整个板块不高于上方 Banner，短列表不留大块空白，长列表可滚动到第 15 条；0–15 条不占用「显示更多」位置，16 条及以上显示底部入口；发布成功后刷新；「显示更多」与游戏「更多」进入对应分页。管理员能从前台进入后台官方发布、选择重要级横幅，普通用户没有该入口。
- 本轮包含读取与前台呈现、正文换行规范化及举报通知修补，不重复账务、迁移和维护停站实验；基础实现的对应验证保留。后端与正文规范化测试 99 项、前端与刷新测试 64 项的既有验证保留；最新布局的 32 项组件测试及类型检查通过。Chrome 1440/390/320 验证桌面正文独立于右栏、手机正文固定第二行且跨文字与操作列、头像独立列、64px 时间列、28px 头像与统一正文行高、首行元素对齐及隐藏按钮时的预留空间；首页与完整列表使用同款 HeroUI v2 默认 Spinner。另以 Chrome 验证 1024/1280/1440/1920 桌面及 390 手机手机 14px / 24px、桌面 16px / 24px 的正文排版，两套站点主题的浅色、深色卡片背景一致。最新定点验收覆盖多行正文的 24px 间距、标签与头像的首行对齐、费用标签同排显示与换行隐藏，以及经典滚动条 14px / 8px 的双侧对称留白；悬浮滚动条不额外占位。首页加载与满列表高度均为 300px，短内容自然收缩，「显示更多」位于滚动列表末尾并进入独立页面。连续三次延迟刷新中，15 条消息与公告始终保留，板块高度保持 300px；真实时间边界、超过浏览器计时范围的公告、恢复可见/切换作用域的旧响应和失败退避有定向回归。浏览器写请求全部被拦截或模拟，未向开发库写入；最终外观待站长验收。
- 失败：公开历史被截断或遗漏、置顶重复/超出名额、不可见消息泄露、官方到点不更新、缓存混用、首页主操作不可达或移动端横向溢出。

### E02-03 举报过渡、恢复、权限与缓存

- 目的：证明小喇叭举报能进现有举报载体并被真实处理与恢复，权限一处不漏，空 `patch` 不破坏旧读取面，处置后各读取面按声明时间更新。
- 前提：隔离库执行完整 sync（含 `patch_id DROP NOT NULL`）；预造若干存量评论举报与评价举报行；账号覆盖 `role` 1、2、3 与未登录会话；多进程运行以复现部署形态。
- 步骤：①举报一条未关联游戏与一条关联游戏的消息，断言举报行 `patch_id` 分别为空与有值；②同一举报人重复举报；不同举报人依次达阈值；两人并发提交临界的第三与第四条；③按 D3 已定口径举报官方消息并凑够阈值人数，再用 `resolve` 结案；另对一条作者已自删的被举报消息执行 `resolve`；再按 ID 直接举报一条未生效的官方消息、一条已撤回的官方消息、一条作者已自删的消息与一条已删除的消息各一次，并对一条隐藏中的消息追加一条在途举报；④权限矩阵：未登录与 `role` 1、2 访问站方列表、官方发布、处置接口，`role 3` 访问同一组接口，另确认旧举报只读路由仍要求 `role >= 4`、旧处理路径对小喇叭返回引导而不误处理；⑤站方对同一目标并发两次 `remove`；对另一目标先 `hide` 再驳回举报；⑥处置后依次请求首页模块、小喇叭页、关联条、横幅、作者个人页、控制台列表，记录首次反映变化的时刻与响应头；⑦回归：处理一条存量评论举报与一条评价举报，断言状态、通知、评价统计重算与条目缓存失效与改动前一致；⑧断言小喇叭举报出现在 01 的收件箱来源里，且控制台待复核队列按最早待处理举报时间正序可达，最老的待办不会被挤掉；⑨断言空 `patch` 的举报在旧列表、详情组件（`ReportCard`、`ReportHandler`）、通知链接三处都不报错、链接指向小喇叭；⑩在站方处置事务执行期间并发提交一条新举报，断言它要么被本次结论覆盖并通知、要么保持待处理留给下一次；⑪用 `role 4` 账号打开旧举报只读路由做正例、`role 3` 做反例；⑫对一条消息先 `hide`，断言它仍在待复核队列、只有作者收到通知，再对它提交 `resolve`（应被拒），最后用 `restore` 或 `remove` 结案。
- 通过：未关联游戏的举报正常落库；重复举报被拒；阈值达成只隐藏一次、作者只收一条通知；超级管理员的普通与官方消息均拒绝新举报且无写入或通知；其余官方消息被举报后进队列但不自动隐藏，官方与已自删消息的举报都能用 `resolve` 结案并通知举报人、不产生恢复与退款，处置期间并发提交的新举报既不漏通知也不重复通知；`hide` 之后举报仍是待办、只有作者收到通知，对 `status = 2` 的 `resolve` 被拒；未生效、已撤回、已自删与已删除的目标都拒绝新举报，隐藏中的目标接受在途举报且不触发第二次隐藏；旧举报只读路由对 `role 3` 拒绝、对 `role 4` 放行；`role` 1、2 与未登录对站方接口全部被拒、`role 3` 全部可用、旧路由门槛不变；并发 `remove` 只生效一次且 `admin_log` 只一条；驳回后消息恢复、作者与举报人各收到对应通知、余额 +50 且只一次；六个读取面在响应头声明的时间内反映变化；存量举报回归无差异；空 `patch` 全链路无异常。
- 失败：任一低权限账号成功执行站方动作、官方消息被自动隐藏、并发处置产生两次通知或两次退款、旧举报流程因 `patch_id` 可空而报错或丢链接、某读取面在声明时间之后仍是旧值。
- 时间点：M02-4 完成、迁移在隔离库演练之后，本批上线前。
- 证据与清理：举报行与消息行快照、通知列表、`admin_log` 导出、权限矩阵逐格结果、各读取面时间戳与响应头、存量举报回归对照。结束后 drop 隔离库、清 Redis、移除测试通知。

三个实验按主覆盖归属分工：账务主覆盖在 E02-01，时间与可见性在 E02-02，权限与举报闭环在 E02-03；E02-03 里出现的退款只是驳回举报流程的连带断言，不重复 E02-01 的账务边界。不新增测试平台、不搭压测框架、不为每个按钮建实验。

## 10 实施任务与验收

| 任务             | 落点                                                                                                                                                                                                                                                                   | 完成判据                                                                                                                                   |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| M02-1 数据与付费 | `prisma/schema/shoutbox.prisma`、`validations/shoutbox.ts`、`constants/shoutbox.ts`、`app/api/shoutbox/**`、`constants/moemoepoint.ts`                                                                                                                                 | 发布、编辑、自删的条件更新与幂等键落地；规则展示与真实扣费一致；E02-01 通过                                                                |
| M02-2 展示与保留 | `app/shoutbox/**`、`app/(site)/user/[id]/shoutbox/**`、`components/shoutbox/**`、[components/home/Container.tsx](../../components/home/Container.tsx)、[components/patch/header/Container.tsx](../../components/patch/header/Container.tsx)、`utils/shoutboxTime.ts`   | 四个展示面按 5.1 的分页与可见性规则工作；无任何删除路径；E02-02 的分页与保留部分通过                                                         |
| M02-3 官方发布   | `app/api/admin/shoutbox/**`、控制台小喇叭页、前台官方发布跳转入口与前后台横幅 | 区间可设，已生效可提前结束、未生效可撤回；置顶占一个名额；横幅按消息记忆关闭并覆盖前后台；E02-02 的置顶与横幅部分通过 |
| M02-4 举报与缓存 | [prisma/schema/patch-report.prisma](../../prisma/schema/patch-report.prisma)、[app/api/admin/report/service.ts](../../app/api/admin/report/service.ts)、`validations/admin.ts`、后台举报组件、`app/api/shoutbox/report/**`、`app/api/shoutbox/cache.ts`、01 收件箱来源 | 举报有接收端、处理端、恢复端与结案端（含官方与作者已自删的消息）；3 人阈值取自单一常量，生产词表为空且过滤关闭；E02-03 通过                |
| M02-5 维护演练   | 运维文档维护清单                                                                                                                                                                                                                                                       | 清单可执行并完成一次演练记录                                                                                                               |

还需满足总计划第 8 节的通用上线条件。本模块特有硬条件是迁移必须先在隔离库完成一次完整 preflight/sync/postflight 演练。第 12 节规则已经定稿，首批关键词词表未提供前过滤保持关闭。

## 11 非目标

不做回复、点赞、收藏、转发与编辑历史展示；不关联资源、不做多游戏关联；不做内容用途审查；不做动态定价、调价、频率限制与同文去重；不做站内群发与邮件群发；不改评论与评价的举报创建端；不新建举报表、工单表、关键词表或规则配置后台；不引入消息总线、队列或插件体系；不改 MDX 体系、不新增规则页面（完整规则随站点文档统一编写）；不做应用内维护模式与停用页（属阶段 6）；不改首页聚合与渲染模式、共享匿名缓存层、`middleware`、账务服务与既有周期任务；不新增依赖。

## 12 已定规则

对应总计划 D3，已于 2026-09-12 由站长确认。误判退款每条最多一次是「累计退款不超过实付」这条账务上限的直接结果。

1. 游戏页关联条名称为「关于本作的小喇叭」。
2. 3 名不同举报人触发用户消息自动隐藏。
3. 关键词命中时在写库和扣费前拒绝；词表只收站长指定的少量词。首批词表尚未提供，因此生产词表保持为空、过滤关闭；测试可注入专用词证明拒绝分支不写库也不扣费。
4. 超级管理员的普通与官方消息禁止新举报；其余官方消息保留举报入口并进入复核队列，但永不自动隐藏；站方只可用 `resolve` 结案并通知举报人。

站长于 2026-09-12 确认：首页 15 条手动滚动列表，全站与游戏每页 20 条并可翻阅全部公开历史；官方消息由前台入口跳转后台发布。价格 50 点、普通编辑窗口 5 分钟、官方默认生效时长 72 小时沿用既有规则。
