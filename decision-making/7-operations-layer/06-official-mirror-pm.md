# 模块 06：官方镜像作为链接（项目管理版）

状态：设计草稿，待站长审阅。技术细节见[实施计划](./06-official-mirror.md)；编排与任务编号见[总计划](./00-master-plan.md)；产品依据见[运营层设计基线](../7.operations-layer-redesign.md)第 5 节。

## 1. 目标与责任人

目标是取消「站长搬运后另建一条资源」这个做法：镜像变成原资源下的一条官方链接，一个资源只有一张卡片，署名留给原发布者，官方链接的可用性归站方、文件内容问题归发布者。同时把前台的「下载次数」改成以揭示为准的「获取次数」，并把历史上已经分裂成两张卡片的存量逐条合并回去。

责任人：站长做设计审定与上线验收；实现由单人开发完成；上线后镜像队列、重新镜像与存量合并由持有资源盘账号的管理员逐条消化（目前只有站长持有）。

## 2. 用户与站方流程

| 角色       | 入口           | 结果                                                                                                                                                         |
| ---------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 访客与用户 | 资源卡片       | 官方链接置顶并标「站方镜像」；存在官方链接时显示「站方已备份」，站方真正运行过才显示「站方已测试」；尚未镜像的社区资源显示「站方备份排队中」                 |
| 发布者     | 资源编辑表单   | 名称、备注、类型、语言、平台照常编辑；官方链接只读，只显示存储、大小与版本，改不动也删不掉                                                                   |
| 发布者     | 资源菜单       | 三个动作：删除我的链接（资源与署名保留）、移交给站方（可选保留署名或显示「用户投稿」）、申请下架（理由必填，期间正常获取与恢复对所有访问者关闭，等站方处理） |
| 站方       | 后台镜像队列   | 按分层顺序取一条，走「待下载 → 已下载待上传 → 已上传待建链接 → 完成」，最后一键添加官方链接                                                                  |
| 站方       | 后台待合并队列 | 先询问发布者「内容相同 / 内容有变化」，得到回答或 7 天未回应后由管理员决定是否合并                                                                           |
| 站方       | 下架工单       | 同意下架（删除资源，资源盘文件按工单内的人工步骤处理）或拒绝下架（附理由，恢复发放）                                                                         |

## 3. 范围

做：官方链接与链接级权限、卡片结构、发布者三动作、镜像队列与进度流、重新镜像、镜像版本确认、存量合并工具、获取计数切换、现有资源管理页迁入新后台。

不做：不给创作者镜像权限；不建自动搬运、离线下载或新的云存储控制台；不改萌萌点既有规则；不做资源级担保等级枚举；不在前台重复实现合并；不要求上线前清空存量队列。

## 4. 依赖与上线批次

依赖模块 01 的后台、模块 03 的工单、模块 04 的稳定链接 ID 与链接历史。模块 04 若没有先把「删除后重建全部链接」改成按 ID 更新，官方链接就无法被保护，本模块不能开工。本模块是独立的一批上线：新镜像流程、链接权限、下架、版本确认、计数切换与合并工具一起发布；存量合并允许上线后长期消化，不阻塞模块 07。

## 5. 任务与里程碑

| 任务  | 里程碑                                                                                                      | 规模 |
| ----- | ----------------------------------------------------------------------------------------------------------- | ---- |
| M06-1 | 官方标记、测试时间、镜像版本、下架与原署名字段落库，写入处按链接级权限拒绝越权                              | M    |
| M06-2 | 卡片结构与发布者三动作全部可用                                                                              | M    |
| M06-3 | 镜像队列、重新镜像队列、进度流与一键添加官方链接                                                            | M    |
| M06-4 | 待合并队列、询问发布者、7 天超时与合并事务                                                                  | L    |
| M06-5 | 获取计数切换：链接与资源各自的 24 小时口径，点击转为辅助统计                                                | S    |
| M06-6 | 资源管理页迁入新后台，接「未镜像 / 总数」、三个队列的等待与处理时长事实（供指标模块复用）与存量合并操作说明 | S    |

## 6. 验收

一，发布者编辑整条资源既不能覆盖也不能删除官方链接，添加官方链接仍需管理员门槛。二，三个动作的即时效果、署名选择与奖励处理与基线一致；申请下架后正常获取与恢复对所有访问者关闭、已获取的也不恢复，站方仍能按权限查看与处理，但已经复制到本地的站外地址无法收回。三，队列不把已在对象存储上的文件当作待镜像，也不把存量候选算成未镜像。四，每一对候选都先询问发布者；合并后点赞、获取次数、仍在有效期的授权与工单指向都落在保留的那张卡片上，且不误删资源盘文件。五，同一个人拿到同一资源的多个镜像只计一次获取，重复请求与恢复不增量，旧点击不再累加同一个展示数字，旧数值原样保留。六，上线后由站长实际处理若干条队列项与至少一对存量合并，并记录处理结果。

## 7. 风险与未决事项

- 站内目前只有对资源盘公开分享的只读调用（用来算文件大小），没有接入删除或离线下载，「同意下架」和合并都不会自动删文件；流程改为「确认文件已处理 → 在一个事务内删除资源、回退奖励、结案与通知」，本模块只保证站内数据正确。人工删文件的做法属 D7 候选，未定前不当成已批准；资源盘自身是否具备离线下载、哈希去重与低频存储能力，仍要按其官方文档核对后留一段结论。
- 官方字段回填边界、移交时使用的站方账号、合并后的引用与统计口径（含合并去重掉的重复点赞不追溯结算）、资源盘研究项范围仍是 D7。未定前不写死回填 SQL，也不把合并按钮的默认行为定为已批准。
- 合并与资源删除一旦提交，应用回退不能把卡片拆回两张；回退顺序必须先关闭合并与下架入口，再处理代码。
- 计数口径切换后增长会看起来变快（揭示通常多于点击），这是基线已接受的结果，不重算历史。

## 8. 实验要证明什么

| 编号   | 业务判据                                                                                                                                                 |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E06-01 | 发布者与管理员各自能做与不能做的事完全按权限落地；三个动作的即时效果、署名选择与奖励处理正确；下架期间正常获取与恢复对所有访问者关闭，站方按权限仍可处理 |
| E06-02 | 队列口径不漏不重，分层顺序符合基线；审核通过与镜像解耦，前台文案与真实状态一致                                                                           |
| E06-03 | 每对候选先问后合；合并的引用迁移一项不漏，失败整对回滚；不误删文件，不靠代码回退拆分已合并数据                                                           |
| E06-04 | 获取计数按访问者对链接、对资源各自 24 小时去重，换版本与跨授权边界都不重复计；重复请求与恢复不增量；点击只进辅助统计；旧数值保留                         |

四个实验全部尚未执行，本文只给判据。实验执行结果与文档交叉审阅分别记录，不互相代替。
